# 재발방지 — **게임을 넘어 반복되는 사고와 막는 장치**

상위: [PD](PD.md) (PD)

**이 문서는 게임 바깥에 산다.** 원장(`HtmlToUnity_작업내역.md`)의 회고록은 **그 게임 안에서 끝난다** —
원장은 게임마다 새로 만들기 때문이다. 그대로 두면 두 번째 게임이 첫 번째 게임의 실패를 그대로 반복한다.
**회고에서 나온 대책 중 다음 게임에도 그대로 통하는 것만** 여기로 올라온다.

## 여는 때

| 언제 | 무엇 |
|---|---|
| **착수 전** | 전체를 한 번 훑는다 (PD 「문서 로딩 규칙」의 「단계와 무관하게 착수 전에 항상 읽는 것」) |
| **패스 ③ 완료 후** | 그 회차 회고록에서 **게임을 넘어 통하는 대책**을 골라 등재한다 |

## G7 과 역할이 다르다 — 둘 다 돈다

| | 무엇인가 | 언제 작동하나 |
|---|---|---|
| **G7 패러다임 스윕** (PD) | **처음 보는 게임에서도 돌아가는 절차** | 매 회차 전수로 센다 |
| **이 문서** | 실제로 났던 사고의 **사후 기록** | 같은 자리를 다시 밟기 전에 |

G7 이 범용 보호막이고, 이 문서는 그 그물을 빠져나간 것들의 목록이다. **하나가 다른 하나를 대신하지 않는다.**

## 등재 규칙 — 안 지키면 이 문서 자체가 오염원이 된다

이 세트의 금칙([SKILL.md](../SKILL.md) 「이 문서 세트에 적을 때의 금칙」)이 여기에 그대로 적용된다.
**지난 게임의 사실을 다음 게임의 사양으로 읽히게 만드는 것**이 이 문서가 죽는 방식이다.

| 규칙 | 이유 |
|---|---|
| **고유명사 금지** | 유닛명·화면명·파일명 대신 **역할 이름**(`선택 카드` · `상점 창`). 다음 게임엔 그 이름이 없다 |
| **실측치 금지** | 「노드 564개」 같은 수는 그 게임의 것이다. 수치는 **원장**에 남긴다 |
| **한 줄에 「재발 조건 + 막는 장치」** | 「주의하겠다」는 등재 대상이 아니다. **도구 · 체크리스트 · 게이트** 중 하나여야 한다 (분석로직 「회고록」) |
| **그 게임에서만 나는 사고는 안 올린다** | 원장 회고록에 남긴다. 여기 올리면 다음 게임이 없는 함정을 피하느라 시간을 쓴다 |
| **등재하면 어느 파트가 읽을지 적는다** | 파트 열이 비면 아무도 자기 것으로 안 읽는다 |

## 등재 목록

**현재 144건.** 등재는 학습 단계에서도 난다 — 패스 ③까지 안 가도 올라온다.

| # | 재발 조건 (무엇을 하면 다시 나나) | 막는 장치 (도구 · 체크리스트 · 게이트) | 파트 |
|---|---|---|---|
| 1 | **웹폰트를 쓰는 원본에서 DOM 크기를 잰다** — 폰트는 비동기로 늦게 뜨므로 첫 측정이 **폴백 폰트의 값**이다. 숫자가 그럴듯해서 의심할 이유가 없다 | **체크리스트** — 원본 DOM 실측은 반드시 `await document.fonts.ready` **뒤에** 한다. 폭·높이가 글자에 의존하는 요소는 폰트 로드 전후를 **두 번 재서 같은지 확인**한다 | UI/UX · FX |
| 2 | **「그 기능이 없다」를 부분 일치 grep 으로 증명한다** — `play` 가 `display`·`scoreDisplay` 에 걸리는 식. **있다고 오판하면 없는 기능을 만들고, 없다고 오판하면 있는 기능을 빠뜨린다** | **체크리스트** — 부재 증명 grep 은 **경계가 있는 정확한 패턴**으로 다시 센다 (`new Audio` · `<audio` · `\.play\(\)`). 세고 나서 **매칭된 줄을 눈으로 본다** | 전 파트 |
| 4 | **하네스를 `requestAnimationFrame` 으로 폴링한다** — 브라우저 창·패널이 화면에 안 보이면 rAF 가 멈춰 **하네스만 정지**한다. 겉으로는 「원본이 안 돈다」로 보여 없는 결함을 쫓게 된다 | **체크리스트** — 원본 폴링은 **타이머**(`setTimeout`)로 한다. 막힌 것 같으면 `document.hidden` 을 먼저 찍고, **원본이 정말 멈췄는지** 타이머 한 번으로 확인한다 (`공통절차.md`) | 전 파트 |
| 5 | **노이즈가 있는 판 위에서 델타를 잰다** — 원본에 자동 진행·무작위가 있으면 재는 동안 다른 것이 움직여 델타에 섞인다. 값이 그럴듯해서 의심할 이유가 없다 | **체크리스트** — 재기 전에 **재려는 변화 외의 변화가 0인 기저 상태**를 만들고, **깔고 방치했을 때 변화가 0** 임을 먼저 증명한다. 무작위가 개입하는 자리는 벡터에 `RANDOM` 으로 표시해 대조에서 뺀다 (`공통절차.md`) | Client · FX |
| 6 | **사람에게 「메뉴를 눌러 주세요」로 넘긴다** — 대화형 에디터 제어가 없으면 자동화가 불가능하다고 단정하게 된다. 그런데 **이관을 의뢰한 사람은 유니티를 모를 수 있고**, 그 순간 절차가 거기서 멈춘다 | **도구** — 에디터 작업은 `-batchmode -executeMethod` 로 직접 실행한다. 「내가 못 한다」고 적기 전에 **배치모드를 먼저 확인**한다 (`공통절차.md`) | 전 파트 |
| 7 | **JSON 행 클래스의 setter 를 `private` 으로 둔다** — 라이브러리에 따라 **안 채워지는데 예외도 안 난다.** 전 행이 기본값이 되고 화면은 「데이터가 비었나 보다」로 보인다 | **체크리스트 + 어설션** — ① 행 클래스 setter 는 `public` ② **행 수 어설션을 반드시 건다** (그게 유일한 방어선이었다) | Design · Client |
| 8 | **비동기 에디터 API 를 배치모드에서 부르고 「됐다」로 넘어간다** — `-quit` 이 먼저 와서 작업이 날아가는데 **성공 로그는 찍힌다** | **체크리스트** — 「요청했다」가 아니라 **결과를 파일로 확인**한다. 동기 경로가 없으면 단계를 나누거나 **패키지를 직접 푼다** | 전 파트 |
| 9 | **프레임워크 골격에 「재생 중에만 되는 호출」을 넣는다** — 게임은 멀쩡히 도는데 **검증 하네스가 그 코드를 못 쓴다.** 그러면 그 계통은 영원히 수치로 판정되지 않는다 | **체크리스트** — 「재생 없이 이 클래스를 세울 수 있나?」를 묻는다. 못 하면 가드를 넣는다 (`GameFramework.md` 「하네스가 쓰는 코드는 재생 밖에서도 서야 한다」) | Client · 전 파트 |
| 10 | **확정표만 채우고 프로젝트 설정에는 반영하지 않는다** — 「모바일·세로」로 확정해 놓고 빌드 타깃이 PC, 화면이 자동 회전인 채로 끝까지 간다. **에디터에서는 잘 돌기 때문에 아무도 모른다** | **게이트 + 검사** — G0 통과 증명에 「확정값이 프로젝트 설정에 반영됐다」를 넣는다. 반영 여부를 **검증 도구가 기계로 확인**한다 (`GameFramework.md` 「확정표를 프로젝트 설정에 반영한다」) | PD |
| 11 | **하네스를 위해 게임 코드에 검증용 진입점을 뚫는다** — 이관 결과물에 치트가 남고, 검증이 **실제 경로를 안 지나** 「그 문은 되는데 진짜 경로는 안 되는」 상태를 못 잡는다 | **체크리스트** — 새 문을 뚫기 전에 **이미 있는 주입 지점**(주입식 RNG · 텍스트 주입 · 틱 인자)으로 되는지 먼저 본다. 재는 도구는 **프로젝트 밖**에 두고 실행할 때만 들여놓는다 (`PD.md` 「게임 코드에 검증용 진입점을 두지 않는다」) | Client · 전 파트 |
| 12 | **엔진 설정에 딸린 구성요소를 짝으로 안 본다** — 입력 처리 방식과 입력 모듈, 색공간과 그래픽 API 처럼 짝이 있는 자리. 어긋나도 **컴파일·씬 열기·「있다」 검사가 전부 통과**하고 **재생하는 순간** 죽는다 | **검사** — 「있다」가 아니라 **「짝이 맞다」**로 쓴다. 「설정이 A 면 A 용 구성요소가 있고 B 용이 없다」. 재생에서만 터지는 것을 **정적 검사로 끌어내리는 것**이 대책이다 (`GameFramework.md` 「엔진 설정에는 딸린 구성요소가 있다」) | Client · PD |
| 13 | **편집 모드에서 UI 레이캐스트를 쏴 「안 맞았다」를 결함으로 읽는다** — 재생하지 않으면 캔버스가 한 번도 그려지지 않아 모든 그래픽의 «그리기 깊이»가 미정으로 남고, 레이캐스터는 그것을 전부 건너뛴다. **버튼도 칸도 똑같이 안 맞아** 마치 무엇이 가로막은 것처럼 보인다 | **도구 + 대조군** — 입력 판정은 **재생 모드 검사**로만 한다 (배치에서 재생에 들어가 실제 포인터 이벤트를 흘린다). 그리고 검사에는 **「사람이 눌러서 된다」고 아는 대상**을 대조군으로 같이 쏜다 — 대조군까지 안 맞으면 그건 게임이 아니라 **검사 환경**이다 | Client · UI/UX |
| 14 | **자동 검사에서 「프레임 수」로 기다린다** — 화면 없는 재생은 초당 수천 프레임이 돈다. 「60프레임 기다렸는데 아무 일도 없다」가 실제로는 **0.01초**여서, 멀쩡히 도는 주기 로직이 **멈춘 것으로 보인다** | **체크리스트** — 시간에 걸린 것(틱·타이머·연출)을 기다릴 때는 **시간으로 기다린다**. 기다린 뒤 **실제 경과 프레임 수를 같이 찍어** 환경이 어떤 속도인지 로그에 남긴다 | 전 파트 |
| 15 | **기준 해상도를 폭 하나로만 맞춘다** — 화면 비율이 기준과 다르면 **세로로 흘러넘쳐 콘텐츠가 화면 밖으로 나간다.** 화면 위쪽 요소는 멀쩡히 보이고 눌리므로 「입력이 안 먹는다」로 보인다 — 실제로는 **거기 없는 것**이다 | **설정 + 검사** — 기준 해상도가 항상 화면 안에 들어오는 방식(`Expand`)을 쓰고, **배선 검사가 그 값을 기계로 확인**한다. 「안 눌린다」는 신고는 **입력이 아니라 배치부터** 의심한다 | UI/UX · Client |
| 16 | **크기를 스스로 정하는 부품을 레이아웃 그룹의 «자식»에 붙인다** — 부모가 자식 크기를 정하는 동시에 자식이 제 크기를 정하면 서로 덮어쓰며 **크기가 폭주한다.** 실제로 버튼이 화면을 통째로 덮었는데 **「있다」·「최소값」 검사 78항이 전부 통과**했다 | **검사** — 「있다」가 아니라 **「재본다」**. 기준 해상도의 임시 캔버스에 프리팹을 세워 레이아웃을 강제로 확정한 뒤, 요소 크기가 **부모와 화면 안에 드는지**를 기계로 센다. 크기는 재야 안다 | UI/UX · Client |
| 17 | **모양 마스크로 알파를 «덮어쓴다»** — 둥근 모서리를 씌우는 코드가 원본의 반투명을 통째로 갈아 끼워 **불투명하게 구워진다.** 모양은 맞으니 눈으로는 안 보이고, 원본을 나란히 놓아야 「배경이 안 비친다」가 보인다 | **자산 검사 + 절차** — 모양 마스크와 원본 알파는 **곱한다**. 구운 자산의 **알파 값을 검사가 직접 읽어** 원본 값과 대조한다 | 연출 · 아트 |
| 18 | **Linear 프로젝트에서 반투명·그림자를 «원본 값 그대로» 굽는다** — 원본(브라우저)은 감마 공간에서 섞고 우리는 Linear 에서 섞어 **결과 색이 다르다.** 반투명 면이 원본보다 밝게 나오는데, 값은 「원본대로」라 의심할 이유가 없다 | **계산 + 주석** — 대표 배경 위에서 ① 감마 합성 결과(정답)를 구하고 ② Linear 합성이 그 값이 되는 알파를 **찾아서** 굽는다. 굽는 스크립트에 «왜 원본 값이 아닌지»와 **최대 채널차**를 남긴다. 눈대중으로 진하게 하지 않는다 | 연출 · 아트 |
| 19 | **굽는 스크립트를 임시 폴더에만 두고 결과물만 커밋한다** — 아트를 **다시 못 굽는다.** 고칠 일이 생기면 「사람이 그림 편집기로」로 돌아가고, 그 순간 자산이 무엇을 근거로 만들어졌는지도 사라진다 | **자리** — 「구성하는 도구」(굽는 스크립트·빌더·임포트 설정)는 **저장소 안**에 둔다. 「재는 도구」만 밖에 둔다. 둘을 가르는 기준은 **「없으면 사람 손으로 돌아가는가」**다 | 아트 · 전 파트 |
| 20 | **9-slice 경계만 넣고 스프라이트 PPU 를 안 맞춘다** — 경계는 스프라이트 PPU 와 캔버스 기준 PPU 의 «비»로 환산된다. 어긋나면 경계가 그만큼 줄어 **둥근 사각형이 타원으로 늘어난다.** 「스프라이트가 있다」·「경계가 있다」 검사는 전부 통과한다 | **검사** — 두 값이 **같은지**를 센다 (`#12` 「짝이 맞다」의 자산판). 모양이 이상하면 **먼저 이 짝을 본다** | UI/UX · 아트 |
| 21 | **「검사가 다 통과했으니」로 재는 도구를 지운다** — 통과 여부와 «다 쟀는지»는 다른 것이다. 미측정 칸이 남아 있으면 그 다음 회차에 **채점기를 처음부터 다시 만들어야** 한다 | **체크리스트** — 지우기 전에 ① 전 검사 통과 ② **「미측정」·「재측정 대기」 칸이 비어 있음** 둘 다 확인한다. 하나라도 남으면 이르다 | PD |
| 22 | **원본의 「간격」을 적어 둔 값으로 읽는다** — 흐름 배치(`space-between` 등)에서는 간격이 **남는 공간을 나눈 결과**다. 그걸 균일 간격으로 근사하면 «어디는 붙고 어디는 떨어지는» 원본의 모양이 사라진다. 게다가 음수 마진처럼 **겹치게 만드는 값**은 그냥 지나친다 | **절차** — 간격을 옮기기 전에 **「이 간격을 만든 규칙」을 원본 CSS 에서 확인**한다. 값이 아니라 **규칙을 옮긴다** (배분은 늘어나는 빈 칸으로, 음수 마진은 음수 간격으로). 옮긴 뒤 **실측해서 모양 셋**(겹치나 · 나머지가 서로 같나 · 실제로 벌어지나)을 센다 | UI/UX |
| 23 | **검사 하네스가 초기화를 «직접» 대신한다** — 재생이 아니면 안 도는 초기화를 두고 검사가 창을 직접 열어 버리면, <b>게임의 진입 경로가 끊겨 있어도 통과</b>한다. 검사가 아니라 자기 자신을 검사하게 된다 | **체크리스트** — 대신 부를 때는 **게임 코드가 부르는 그 호출**을 그대로 쓴다 (화면 전환 함수 등). 창·컴포넌트를 직접 만지지 않는다 | Client · 전 파트 |
| 24 | **「어차피 안 보이니 괜찮다」로 넘긴다** — 원본의 남는 상태·죽은 값은 <b>정말 영향이 없는지 실측해야</b> 안다. 추론으로 넘기면 그 물음표가 회차마다 되살아나고, 나중에 원본이 바뀌면 어디가 어긋났는지 못 찾는다 | **벡터** — 「영향 없음」도 **벡터로 등재**한다 (입력 순서 → 변화 0). 그리고 <b>상태 자체는 그대로 옮긴다</b> — 안 보인다고 지우면 상태 기계가 갈린다 | Client · Design |
| 25 | **「구성하는 도구니까 남긴다」에서 멈춘다** — 남기는 것은 맞지만 **자리가 틀리면** 저장소 공용 자리에 그 게임 전용 도구가 남는다. 다음 게임 담당자는 **지워도 되는지 판단할 수 없고**, 결국 「무서워서 못 지우는 것」이 쌓인다 | **자리 규칙** — 어느 게임에도 안 묶인 것만 공용 자리에 둔다. **그 게임 전용은 그 게임 폴더로.** ⚠ 굽는 도구를 옮기면 **결과 픽셀이 달라질 수 있다**(라이브러리마다 안티에일리어싱·블러가 다르다) — 옮긴 뒤 **렌더 판정을 다시 재고** 끝낸다 | 전 파트 |
| 26 | **시키지 않은 깃 조작을 한다** — 「정리된 김에」 커밋하거나 브랜치를 파서 옮겨 둔다. ⚠ **브랜치를 옮기는 순간 그 브랜치에만 있던 파일이 작업 트리에서 사라진다** — 사람이 보고 있던 상태가 통째로 바뀐다. 실제로 그렇게 사라져 커밋에서 되살려야 했다 | **규칙** — 커밋·브랜치·푸시는 **사람이 말한 그 범위만**. 결과를 어디에 남길지는 **사람의 결정**이고, 우리가 하는 것은 «남길 수 있는 상태로 만들어 두는 것»까지다 | 전 파트 |
| 27 | **배경을 창 루트에 `Image` 로 붙여 배경 삼는다** — 결과는 맞아 보인다(안전 영역 자식보다 먼저 그려지니까). 그런데 **「BG 가 어디 있나」가 구조에서 안 보이고**, 검사가 셀 대상이 없어진다. 다음 사람이 배경을 옮기다 **안전 영역 안에 넣어도 아무도 모른다** | **구조 + 검사** — 창은 언제나 `Window → BG · SafeArea` 다. 배경은 **이름 있는 형제**로 만들고, 검사가 ① BG 가 SafeArea 밖인가 ② 먼저 그려지는가 ③ SafeArea 안에 꽉 찬 이미지가 없는가를 센다. 배경이 없는 창은 **없는 이유를 주석으로** 남긴다 | UI/UX |
| 28 | **안전 영역을 «네 변 전부»에 물린다** — 세로 게임에서 좌우 안전 영역은 대개 0이라 티가 안 나는데, **0이 아닌 기기에서 쓸 수 있는 폭이 줄어든다.** 개발기에서는 영영 안 보인다 | **선택** — 적용할 변을 **고를 수 있게** 만든다(상·하 / 좌·우 / 전부). 세로 게임의 기본은 **상·하**다 | UI/UX |
| 29 | **유니티 용어로 선택지를 준다** — 「Tilemap 으로 할까요?」는 «아는 사람만 답할 수 있는 시험»이다. 이관을 의뢰한 사람은 유니티를 모를 수 있고, 모르면 **아무거나 고른다.** 그 답이 확정표에 박혀 끝까지 간다 | **묻는 법** — ① 용어 옆에 **무엇인지 한 줄** ② **가능한 방식을 최대한 다** 편다 ③ **고르면 무엇이 달라지나**(비용·품질·나중에 바꾸기 어려운가)를 적는다 ④ **지금 안 정해도 되는 것**은 그렇게 말한다 | PD |
| 30 | **「일단 여기까지」로 남은 것을 만든다** — 미측정 칸·미흡 목록·안 지운 도구가 쌓인다. 다음 회차는 **그 목록부터 읽어야** 하고, 목록은 회차마다 길어진다 | **완료 조건** — 끝낼 때 셋을 같이 본다: ① 게이트 숫자에 빈 칸·「미측정」이 0 ② 「재측정 대기」가 비었다 ③ 이관에 쓴 도구·임시물이 없다. **남은 것이 있으면 「완료」가 아니다** | 전 파트 |
| 31 | **하네스에서 «시간을 되감는다»** — 시나리오마다 타임스탬프를 0으로 되돌리면, 원본이 들고 있는 «직전 시각»은 그대로라 **dt 가 음수**가 된다. 개체가 **반대로 튀고**, 그것이 «원본 이상»으로 읽힌다 (실측 −0.983초 · 7.87칸) | **규칙** — 하네스가 주는 **타임스탬프는 단조 증가로만** 준다. 상태를 되돌릴 때도 시간은 되감지 않는다. ⚠ 이상해 보이는 값을 만나면 **원본을 의심하기 전에 하네스를 의심한다** — 우리가 넣은 값이 원본에 없던 조건을 만들었을 수 있다 | Client · 전 파트 |
| 32 | **텍스처 타입만 Sprite 로 바꾸고 «스프라이트 생성 모드»를 안 바꾼다** — 임포터는 「Sprite 다」라고 답하는데 스프라이트 로드는 **null** 이다. 씬은 멀쩡히 구워지고 **화면만 빈다** | **검사** — 구운 뒤 «로드해서» 확인한다. 임포터 설정을 믿지 않는다. 스프라이트를 쓰는 자리는 «참조가 비었는지»를 기계로 센다 | 아트 · UI/UX |
| 33 | **파일시스템으로 만든 폴더에 에셋을 만든다** — 에셋 파이프라인이 그 폴더를 모르면 **조용히 실패**한다. 에러도 로그도 없다 | **절차** — 폴더는 **에셋 API 로** 만든다. 그리고 만든 뒤 «몇 개가 실제로 생겼나»를 세어서 찍는다 — 「만들었다」는 증거가 아니다 | 전 파트 |
| 34 | **카메라가 둘인데 «각자 무엇을 그릴지»를 안 가른다** — 뒤 카메라가 **월드를 자기 시점으로 다시 그린다.** 화면에는 엉뚱한 배율의 그림이 뜨는데, 데이터도 로직도 다 정상이라 원인을 딴 데서 찾게 된다 | **설정** — 카메라마다 **그릴 대상을 좁힌다**(레이어). 그리고 겹쳐 그리려면 그 파이프라인이 요구하는 «겹치기 방식»을 쓴다 — 지우기 설정만으로는 안 겹쳐지는 파이프라인이 있다 | 연출 · UI/UX |
| 35 | **HUD 창에 «불투명 배경»을 깐다** — 창 구조 규칙(배경은 안전영역 밖)을 기계적으로 따르다 보면, **배경이 게임 화면을 통째로 가린다.** 규칙은 «배경이 있는 창»의 규칙이지 «배경이 필요 없는 창»에 배경을 만들라는 말이 아니다 | **판단** — 창을 만들 때 「이 창은 무엇을 가려야 하나」를 먼저 묻는다. 가릴 것이 없으면 **배경을 두지 않고, 없는 이유를 주석으로 남긴다** | UI/UX |
| 36 | **배치 실행의 «명령 출력»만 보고 넘어간다** — 출력은 잘린다. 실패 로그가 그 뒤에 있으면 **못 보고 다음 단계로 간다.** 실제로 「스프라이트를 못 읽었다」가 로그에만 있었고, 그 사이 엉뚱한 곳을 세 번 고쳤다 | **절차** — 판정은 **로그 «파일»을 grep** 해서 한다. 명령 출력은 참고일 뿐이다 (`공통절차.md` 「배치 로그는 파일로 받는다」) | 전 파트 |
| 37 | **두 번째 게임에서 «화면 방향·해상도»를 이월한다** — 첫 게임의 확정을 「프로젝트 규약」으로 착각한다. 확정에는 «저장소 규약»(배관 — 이월)과 «게임의 결정»(모양 — 매번 묻기)이 있는데, **방향·해상도는 게임의 결정**이다. 가로 게임을 세로 틀에 박으면 그때 깨닫는다 | **절차** — 새 게임 착수마다 「세로/가로? 해상도 몇×몇?」을 **반드시 묻는다.** 이월해도 되는 것은 PD 의 「이월되는 것」 표에 있는 것뿐이다 | PD |
| 38 | **재는 동안 쓰는 파일(캡처·좌표·하네스)을 유니티 `Temp/` 에 둔다** — 유니티가 «시작할 때 Temp 를 비운다». 배치를 한 번 돌리면 원본 캡처가 사라지고, 대조를 처음부터 다시 만든다. 「배치 로그」 함정과 같은 뿌리인데 **로그가 아닌 파일에도 똑같이 적용된다는 걸 놓친다** | **자리** — 재는 동안 쓰는 파일은 **유니티 프로젝트 밖**(스크래치패드·별도 폴더)에 둔다. `Temp/` 에는 «그 배치 실행 안에서만» 쓰는 것만 둔다 | 전 파트 |
| 3 | **원본 코드를 읽고 「이건 버그니 고쳐서 옮기자」로 넘어간다** — 원본의 이상 거동은 **실측하면 재현되는 사양**이다. 고치면 파리티 판정이 그 지점에서 무너지고, 무엇이 원본이었는지가 사라진다 | **게이트** — 원본 이상은 **실측으로 확증하고 「원본 이상 목록」에 등재**한 뒤 PD 가 재현/수정을 확정한다. 이관 담당자가 판단하지 않는다 (`PD.md` 「원본의 이상 거동」) | Client · Design |

| 39 | **표본 하나를 재고 「게임 상수」라고 적는다** — 한 스테이지·한 화면만 재면 그 값이 **전역인지 그 스테이지의 것인지 구분되지 않는다.** 전역으로 적으면 다음 스테이지부터 전부 어긋나는데, **첫 스테이지는 맞으므로 검증이 통과한다** | **체크리스트** — 「상수」·「전역」이라고 적기 전에 **두 번째 표본**을 잰다. 못 재면 그 값에 **`(표본 1 — 전역 여부 미확인)`** 을 붙인다. 스테이지·레벨·화면이 여럿인 게임은 **파라미터가 그 단위로 갈릴 수 있다는 것이 기본 가정**이다 | 전 파트 |
| 40 | **진행 상태를 «이벤트가 도착한 시각»으로 추론한다** — 원본이 내보내는 진행 이벤트는 연출이 끝난 뒤에 오므로 지연이 크고, 그 지연이 화면·기기·구간마다 다르다. 시간 창으로 귀속하면 **앞선 시행의 결과가 지금 시행에 붙고**, 그 결과가 **「원본이 저절로 진행됐다」는 없는 이상**으로 보고된다 | **체크리스트** — 상태는 **관측 가능한 상태값을 직접 읽어** 확정한다(런타임 상태·화면의 상태 표기). 이벤트를 써야 하면 시간 창이 아니라 **상태 전이**로 귀속하고, **시행 1 ↔ 결과 1** 이 성립하는지 세어 본다. 안 맞으면 그 회차 데이터는 버린다 | 전 파트 |
| 41 | **캡처·덤프 파일명을 「다음 단계로 갔겠지」로 붙인다** — 실제로 안 넘어간 회차가 섞이면 **파일명과 내용이 어긋난 채로 다음 회차의 근거가 된다.** 발견하고 파일명을 고치면 **사고가 있었다는 사실 자체가 사라진다** | **체크리스트** — 파일명은 **확정된 상태값으로** 붙인다(위 40번). 이미 어긋난 것을 발견하면 **파일명을 고치지 말고 대조표를 남긴다** | 전 파트 |

| 42 | **확정표에 «행 자체가 없는» 값이 코드에 박힌다** — 빈칸은 「미확인」으로 보이지만 **행이 없으면 빈칸으로도 안 보인다.** 그래서 아무도 묻지 않고, 첫 게임에 누군가 코드에 적은 값이 다음 게임에도 그대로 남는다. 실제로 게임을 두 개 넘기는 동안 안 잡혔다 | **체크리스트** — 게임을 넘길 때 **「이 게임의 것으로 바뀌어야 하는데 코드에 하드코딩된 값」을 한 번 센다**(제품명·식별자·표시 이름·경로 접두어 등). 하나라도 나오면 **확정표에 행을 만들어** 다음 게임부터 물어지게 한다 | PD |
| 43 | **여러 게임이 한 프로젝트에 사는데 «프로젝트 전역 설정»을 게임 하나 기준으로 박는다** — 화면 방향·해상도처럼 엔진이 프로젝트당 하나만 갖는 값. 두 번째 게임이 우연히 같으면 안 터지고, **세 번째 게임에서 처음 충돌**한다 | **자리** — 전역 설정을 다루는 도구는 **게임별 프로필을 담고 활성 하나를 적용**하게 만든다. 활성 선택은 **코드·커밋에서 읽히는 방식**으로 한다(감춰진 로컬 상태 금지) — 그래야 「확정표가 설정까지 갔다」를 나중에 검증할 수 있다 | PD · Client |

| 44 | **정답지에 «관측값»과 «유도값»을 같은 표에 섞어 놓고 둘 다 정답으로 쓴다** — 유도값(피팅으로 얻은 속도·계수 등)은 관측값(좌표·시각)보다 오차가 크다. 어긋나 있어도 **표만 보면 안 보이고**, 이관 쪽은 유도값에 맞추다가 **관측과 갈린다.** 실제로 유도 컬럼이 관측과 최대 두 자릿수 % 어긋나 있었고, 그 오차가 **「원본에 이상한 현상이 있다」는 가짜 수수께끼**까지 만들었다 | **채점기** — 채점기가 **정답지부터 검산**한다: 물리·수학으로 서로를 결정하는 컬럼끼리(예: 자유낙하의 최고점 시각 ↔ 초기 속도) 역산해 **자기모순을 먼저 찍는다.** 그리고 정답지에 **어느 컬럼이 관측이고 어느 것이 유도인지 표시**하고, 어긋나면 **관측이 이긴다**를 문서에 못 박는다 | Client · PD |

| 45 | **런타임을 읽을 수 있는데도 «관측값을 피팅»해서 규칙을 만든다** — 피팅은 그럴듯한 표를 낳지만 오차가 남고, 그 오차가 **「구간마다 값이 다르다」는 가짜 구조**를 만든다. 실제로 두 회차에 걸쳐 곡선을 피팅하다가 **원본이 쓰는 변수 하나**(선형으로 차오르고 상한에서 고정)를 찾자 **수십 점짜리 표가 두 줄로** 줄었다 | **체크리스트** — 곡선·표를 만들기 전에 **「이 값을 만드는 변수가 런타임에 있나」를 먼저 찾는다**(수치 변수·회전값·상태 플래그). 찾으면 **기전을 옮기고 표는 버린다.** 못 찾을 때만 피팅하고, 그때는 **피팅임을 표에 표시**한다 (`#44` 와 짝) | Client · Design |
| 46 | **«공칭 입력값»을 축으로 삼아 규칙을 세운다** — 자동화가 「200ms 눌렀다」고 적어도 실제로는 지터·중간 캡처 때문에 더 길다. 축이 오염되면 **같은 값이 구간마다 다르게 보이고**, 「구간별 파라미터가 있다」는 결론까지 간다. 실제로 그렇게 «레벨별 파라미터»를 확정했다가 뒤집었다 | **체크리스트** — 입력 축은 **실제로 일어난 값을 재서** 쓴다(실제 누른 시간·실제 좌표). 재는 행위가 값을 바꾸는지도 본다 — **판정용 시행에서는 중간 캡처를 최소화**한다. 공칭축을 쓸 수밖에 없으면 **바이어스를 측정해 표에 적는다** | 전 파트 |
| 47 | **정답지가 요구하는 «형태»를 채우려고 없는 값을 만들어낸다** — 「구간마다 실패-성공-실패 3점」 같은 규격은 대개 옳지만, **원리적으로 한쪽이 존재하지 않는 구간**이 있다(입력이 상한에 걸려 더 세게 못 하는 경우 등). 억지로 채우면 **거짓 정답지**가 된다 | **체크리스트** — 규격을 못 채우면 ① **부존재를 증명**하거나(상한이 있다 · 상한이 그 구간에도 적용된다 · 상한 상태가 성공한다) ② **미측정으로 남긴다**. 증명되면 **합격 기준을 그 구간만 다른 형태로 닫고 근거를 적는다** | PD · Client |

| 48 | **미측정 파라미터를 «정답지가 통과하도록» 고른다** — 실측하지 못한 계수(반발·마찰·감쇠 등)를 정답지에 맞춰 조정하면 점수가 즉시 오른다. 그런데 그 순간 **정답지는 우리 구현을 검증하는 게 아니라 «우리가 맞춘 값»을 확인**하게 되고, **채점의 의미가 통째로 사라진다.** 게다가 그 값은 실측처럼 보이는 **가짜 근거**로 다음 회차에 인용된다 | **체크리스트 + 게이트** — 미측정 파라미터는 **추정임을 데이터·주석에 표시**하고 **골든에 맞춰 조정하지 않는다.** 그 값 때문에 실패하는 항목은 «결함»이 아니라 **«미측정의 표시»**로 읽고 **재측정 대기에 올린다.** 값을 바꾸고 싶으면 **먼저 재라** — 민감도 실험은 「어디를 재야 하는가」를 고르는 데만 쓴다 | Client · PD |

| 49 | **원본이 «엔진 위에서» 도는지 안 보고 규약을 문자대로 적용한다** — 「엔진 혼입 금지」는 원본이 손으로 짠 계산일 때의 규칙이다. 원본 자체가 물리 엔진 위에서 돌면 **엔진을 안 쓰는 쪽이 원본에서 멀어진다.** 회전 관성·마찰이 붙은 거동은 「반사 벡터」 모델로 **원리적으로 재현 불가**인데, **단발 궤적 검사는 통과**하므로 다중 충돌 뒤에야 갈린다 | **체크리스트** — 착수 3단계에서 **「원본이 엔진(물리·애니·경로) 위에서 도는가」를 런타임에서 확인**한다. 쓰고 있으면 파라미터를 **재지 말고 읽고**, 같은 계열 엔진을 **실측값으로** 쓴다. 규약의 «문자»와 «이유»가 갈리면 **이유를 따르고 규약 문서에 단서를 단다** (`Client.md`) | Client · PD |
| 50 | **물리 엔진의 «스케일»을 원본과 다르게 잡는다** — 물리 엔진은 접촉 슬롭·**반발이 죽는 속도 임계** 같은 **절대 크기 상수**를 갖는다. 원본이 「1 미터 = N 픽셀」로 도는데 우리가 1:1 로 두면 그 임계가 상대적으로 N 배 어긋나 **다중 충돌 뒤 «안 튀기 시작하는 지점»이 갈린다.** 단발 궤적은 멀쩡해서 안 잡힌다 | **체크리스트** — 원본의 **월드 스케일을 실측**하고(엔진 월드 중력 ÷ 화면 단위 중력) 우리 월드 단위를 **거기에 맞춘다.** 데이터는 원본 표기 단위로 두고 **로드 지점 «한 곳»에서 환산**한다 — 환산이 흩어지면 어디가 어긋났는지 못 찾는다 | Client · PD |

| 51 | **엔진이 주는 «편의 환산 함수»를 검산 없이 믿는다** — 좌표 환산·변환 헬퍼가 **일부 요소(원근·부모 변환·스케일 계층)를 반영하지 않고도 «그럴듯한 값»을 조용히 돌려준다.** 그러면 실측이 캡처와 모순되고, 그 모순을 **「미측정」으로 남기게** 된다 — 원인이 도구인데 대상 탓으로 읽는다 | **체크리스트** — 환산 함수의 결과는 **캡처와 한 번 대조**한다(요소 2~3개면 된다). 어긋나면 **함수를 의심**하고 **원시 파라미터로 직접 환산**해 본다. 「모순이라 미측정」으로 넘기기 전에 **모순의 원인이 도구인지 대상인지**를 가른다 | 전 파트 |
| 52 | **「미측정」을 «값이 없다»로만 읽는다** — 실제로는 **모델이 틀려서** 값이 안 맞는 경우가 있다. 데이터로 박혀 있을 줄 알았던 것이 **겹침·이웃 관계로 결정되는 규칙**이거나, 평면인 줄 알았던 것이 **원근**이거나. 값을 더 재도 안 풀리고, 회차만 늘어난다 | **체크리스트** — 미측정이 **한 회차를 넘겨도 안 풀리면** 「덜 쟀나」가 아니라 **「내 모델이 틀렸나」**를 먼저 묻는다. 같은 대상을 **다른 각도로**(관계·순서·계층) 한 번 떠 본다 | 전 파트 |
| 53 | **파생 규칙을 알아내고 «규칙을 계산»하는 코드를 짠다** — 원본이 겹침·순서 같은 **기전**으로 내는 결과를 조건식으로 재현하면, 기전이 자연히 처리하던 **경계(가장자리·예외 배치)에서 갈린다** | **체크리스트** — 파생 규칙을 찾았으면 **그것을 만든 기전이 무엇인지**까지 묻는다. 기전을 옮길 수 있으면 **기전을 옮기고, 파생 규칙은 «교차 검산»으로** 쓴다 — 둘이 어긋나면 무언가 틀린 것이다 | Client · 아트 |

| 54 | **성격이 다른 판정을 «한 점수»로 합쳐 본다** — 「구조·수치가 맞나」와 「결과가 같나」를 한 합계로 내면, 한쪽이 좋아지고 다른 쪽이 나빠졌을 때 **총점만 보고 «개선을 되돌리는»** 판단을 하게 된다. 실제로 물리 정확도가 크게 올랐는데 **다른 원인(미측정)** 으로 결과 판정이 내려가 총점이 비슷해진 일이 있었다 | **채점기** — 채점을 **갈래로 나눠 각각의 수를 낸다**(구조·수치 / 결과). 총점만 보고하지 않는다. 한쪽이 나빠지면 **그 갈래의 원인을 따로** 규명한다 — 다른 갈래의 개선과 상계하지 않는다 | Client · PD |
| 55 | **엔진 API 를 «설정 순서»를 모르고 부른다** — 어떤 값은 다른 값이 켜져 있어야만 받아들여지고, 아니면 **경고만 찍고 조용히 버려진다.** 기본값과 같은 항목은 티가 안 나서 **일부만 어긋난 채로 통과**한다 (실제로 한 개체의 질량이 100배 어긋나 있었다) | **검사** — 설정한 값을 **되읽어 대조**한다(「넣었다」가 아니라 「들어갔다」). 특히 **파생값**(질량·크기·경계)을 굽고 나서 **파일에서 읽어 기대값과 비교**한다. 기본값과 같은 항목도 **빠뜨리지 말고 전부** 센다 | Client · 아트 |

| 56 | **런타임에서 «데이터·속성»만 찾고 «로직»은 안 찾는다** — 오브젝트·좌표·물리 파라미터를 읽어 내고는 **규칙만은 「쏴 보고 역추론」**한다. 그런데 엔진에 따라 **규칙 자체(이벤트 시트·비주얼 스크립트 그래프·상태 기계)가 런타임에 통째로 들어 있다.** 못 찾으면 여러 회차를 관찰에 쓰고도 확정을 못 한다 | **체크리스트** — 런타임을 찌를 때 **①오브젝트 ②속성 ③«규칙»** 셋을 다 훑는다(이벤트/스크립트/그래프 컨테이너를 이름으로 검색). ⚠ **함수 이름이 난독화로 지워져 있어도 포기하지 마라** — 엔진 네임스페이스를 훑어 **함수 동일성으로 역해석**하면 이름이 붙는다 | Client · Design |
| 57 | **「그 조건이 «없다»」를 관찰로 증명하려 한다** — 방향 조건·속도 임계·예외가 **없다**는 것은 **아무리 많이 시행해도 증명되지 않는다.** 못 본 것과 없는 것이 구분되지 않기 때문이다. 그런데 「N 번 해 봤는데 안 그러더라」가 **실측처럼 문서에 적힌다** | **체크리스트** — **부정 사실은 «열거»로만 닫는다** — 조건 목록·규칙 목록을 **읽어서** 확인한다. 못 읽으면 **「미측정(부정 사실은 관찰로 못 닫음)」**으로 남긴다. 시행 횟수는 **긍정 사실**(있다·이렇게 된다)에만 근거가 된다 | 전 파트 |

| 58 | **「우리가 잰 값」을 «원본의 값»으로 착각한다** — 입력 시간·좌표를 우리 시계·우리 좌표계로 재고 그것을 축으로 정답지를 만든다. 그런데 **게임이 실제로 쓰는 값은 따로 있고**(내부 변수), 우리 측정에는 지연·오차가 섞인다. 그러면 **없는 바이어스를 「보정」이라며 발명**하고, 그 보정 위에 만든 정답지 행이 **틀린 채로 인용**된다 | **체크리스트** — 입력·상태를 정답지의 «축»으로 쓰기 전에 **「게임이 이 값을 어디에 담고 있나」를 런타임에서 찾는다.** 있으면 **그 값이 축**이고 우리 측정은 근사다. 못 찾을 때만 우리 측정을 쓰고 **「우리 시계로 잰 값」이라고 표시**한다 | Client · 전 파트 |
| 59 | **한 표본만 뜨고 「이 값은 상수다」로 옮긴다** — 부모에 딸린 요소의 좌표·크기는 **상대값**으로 나오는데, 한 곳만 보면 **절대 상수와 구분되지 않는다.** 그대로 옮기면 **원점 근처로 가거나 크기가 뭉개지는데**, 값 자체는 「일관돼 보여서」 의심을 안 산다 | **체크리스트** — 같은 종류의 요소를 **여러 곳에서 나란히 떠서 대조**한다. 전부 같은 값이면 **상수가 아니라 «상대값»을 의심**한다. 절대 위치는 **경계 상자(bbox)를 정본**으로 쓴다 | UI/UX · 아트 |

| 60 | **연출을 «수치 검사 통과»로 닫는다** — 개체 수·위치·타이밍이 전부 맞는데 **화면이 틀린** 것이 있다. 「지워야 할 때 안 지운 잔상」은 개수에 안 나오고, 「간격이 틀려 뭉친 꼬리」는 개체 수로 안 잡힌다. 그래서 **연출은 만든 뒤 아무도 안 보는 자리**가 되고, 편집 모드로는 **원리적으로** 확인이 안 된다 | **도구** — 연출은 **재생 캡처를 «눈으로 봐서»** 닫는다(상태별로 최소 3장: 시작 · 진행 중 · 끝난 뒤). ⚠ 캡처가 **갈리는 시점**을 골라라 — 두 상태의 그림이 같아 보이는 순간에 찍으면 판정이 안 된다. ⚠ 배치모드에서는 화면 캡처 API 가 **파일을 안 만들 수 있다** — 카메라를 렌더 텍스처에 직접 그려 저장한다 | 연출 · 아트 · Client |
| 61 | **검사 결과가 «저장된 진행 상태»에 따라 갈린다** — 세이브·진행도가 남아 있으면 검사가 전제한 시작 지점과 달라져 **같은 코드인데 점수가 다르게** 나온다. 그러면 다음 회차가 **없는 결함을 쫓거나**, 반대로 **있는 결함을 놓친다** | **체크리스트** — 검사는 **시작 상태를 스스로 만든다**(세이브 초기화 또는 목표 지점까지 결정적으로 이동). 못 하면 **「이 점수는 진행도 X 기준」**을 리포트에 찍는다. 점수가 회차마다 다르면 **코드보다 «시작 상태»를 먼저 의심**한다 | Client · PD |

| 62 | **주기적 연출을 «캡처 격자»로 재고 결론을 낸다** — 표본 간격이 주기의 절반보다 크면 진동이 **«정지»로 보인다**(앨리어싱). 그러면 「이 요소는 안 움직인다」·「이 연출은 없다」가 **실측처럼 문서에 적히고**, 이관 결과에서 그 연출이 통째로 빠진다. 반대로 **한 번 지나가는 물리 거동을 «연출»로 오독**하기도 한다 | **체크리스트** — 연출을 캡처로 재기 전에 **엔진의 «규칙»(타이머·트윈·애니 액션)을 먼저 읽는다**. 캡처로만 재야 하면 **간격을 두세 가지로 바꿔 같은 결론이 나오는지** 본다 — 달라지면 그 결론은 **격자가 만든 것**이다. 「진동 없음」·「연출 없음」은 **부정 사실**이라 열거로만 닫힌다(#57) | 연출 · 아트 |
| 63 | **엔진 문서에 적힌 «열거형 순서»를 그대로 옮긴다** — 이징·블렌드·정렬 같은 열거값의 실제 인덱스가 **문서 순서와 다를 수 있다.** 그대로 옮기면 **그럴듯하게 비슷한 다른 연출**이 나오고(탄성이 단조로 바뀌는 식), 값이 「문서대로」라 의심을 안 산다 | **체크리스트** — 열거값은 **채록해서 이름을 확정**한다(대표 몇 개를 실제로 재생시켜 대조). 확정 못 한 값은 **「인덱스만 알고 이름 미확정」**으로 표시하고 옮기지 않는다 | 연출 · Client |

| 64 | **「값이 흩어진다」를 「그 값은 원래 흩어진다」로 결론낸다** — 계수를 재는데 부호까지 뒤집히며 흩어지면 **「계수 하나로는 안 잡힌다」**로 적고 미측정에 넣게 된다. 그런데 흩어짐의 흔한 원인은 **재는 «축»이 틀린 것**이다(작용점이 다른 곳인데 중심 기준으로 쟀다든지). 축을 바로잡으면 **같은 데이터가 한 값으로 모인다** — 실제로 부호까지 뒤집히던 값이 축을 바꾸자 **35건 중 34건이 0** 으로 닫혔다 | **체크리스트** — 「흩어진다」로 닫기 전에 **① 그 힘·효과가 «실제로 작용하는 지점»이 어디인지** ② **거기서의 상대량으로 다시 계산**해 본다. 그래도 흩어지면 **흩어짐의 «구조»**(두 무리로 갈리나·크기에 비례하나)를 적는다 — 「흩어진다」만 적으면 다음 회차가 같은 자리에서 멈춘다 | Client · 전 파트 |
| 65 | **읽을 수 있는 값을 «유도»한다** — 엔진이 그 값을 이미 갖고 있는데(충돌 법선·접촉 목록·내부 상태) 관측치 차분으로 되돌린다. 유도에는 **수치 오차가 섞이고**, 그 오차가 **「이 값은 원래 산포가 크다」**로 굳어 다음 회차까지 이어진다 | **체크리스트** — 유도하기 전에 **「엔진이 이 값을 들고 있나」를 먼저 찾는다**(#56 의 로직판을 «상태»로 확장). 유도할 수밖에 없으면 **유도값임을 표시**하고, 나중에 직독 경로가 나오면 **산포를 다시 재서** 이전 결론을 검토한다 | Client |

| 66 | **정답지에 «모형이 낸 값»이 섞인다** — 실측을 정리하다가 「이 값이면 이렇게 될 것」이라는 **예측 컬럼**을 같은 표에 넣게 된다. 그러면 채점이 **「원본과 같은가」가 아니라 「그 모형과 같은가」**를 재고, **우리 구현이 원본이 아니라 모형 쪽에 붙어도 «맞았다»**고 나온다. 실제로 그런 행에서 실측과 예측이 서로 반대 판정이었다 | **채점기 + 표기** — 정답지의 **모든 컬럼에 「실측 / 유도 / 예측」을 표시**하고, **예측 컬럼은 채점에서 뺀다**(참고로만). 채점기가 **제외 이유를 화면에 찍게** 한다 — 조용히 빼면 다음 회차가 그 행을 다시 넣는다 | Client · PD |
| 67 | **개선 폭을 «내 변경의 공로»로 몰아 적는다** — 여러 변경이 누적된 뒤 수치가 좋아지면 마지막 변경이 전부를 설명한 것처럼 쓰게 된다. 그러면 **과장된 인과가 문서에 남고**, 다음 회차가 그 「효과 좋은 조치」를 엉뚱한 데 다시 쓴다 | **체크리스트** — 큰 개선을 보고하기 전에 **그 변경만 되돌려 다시 재 본다**(짝지어 실행). 되돌려도 개선이 남으면 **그만큼은 다른 몫**이라고 적는다. 「이 변경이 단독으로 설명하는 것은 X 다」까지 써야 인과가 산다 | 전 파트 |

| 68 | **입력을 «대역»으로 묶어 보고 「비결정적」이라 결론낸다** — 성공 창이 아주 좁으면, 대역(구간·반올림한 값)으로 묶었을 때 **「가끔 되는 것」**으로 보인다. 그러면 「원본이 원래 흔들린다」로 적고 **더 안 판다.** 실제로는 **정확한 값 하나만 되는 «결정적»** 현상이었다 — 대역 안에 성공 1점과 실패 17점이 같이 들어 있었을 뿐이다 | **체크리스트** — 「같은 입력인데 갈린다」가 나오면 **입력을 «가장 정밀한 축»으로 다시 본다**(반올림·구간·평균을 걷어낸다). 갈리는 것이 남으면 그때 **성공률을 n 과 함께** 적는다. ⚠ **대조군을 같이 재라** — 다른 대역이 안정적이면 「원본이 원래 흔들린다」는 그 구간에만 해당한다 | Client · PD |
| 69 | **「이 구간은 원래 불안정하니 우리 실패도 괜찮다」로 덮는다** — 파리티가 안 맞는 자리를 «혼돈 구간»으로 분류하면 조사가 멈춘다. 그런데 **원본이 그 대역에서 안정적인지 재 보지 않았다면** 그 분류는 근거가 없다 | **체크리스트** — 「혼돈이라 약한 구간」이라고 쓰기 전에 **원본을 그 대역에서 n≥8 로 재서 성공률을 낸다.** 원본이 안정적이면 우리 실패는 **진짜 격차**다 — 분류를 철회하고 원인을 판다 | Client · PD |

| 70 | **런타임 코드가 «구워 둔 설정»을 조용히 덮어쓴다** — 값을 주입하는 코드가 옆에 있는 표시 설정까지 기본값으로 되돌린다. **프리팹 검사는 통과한다**(구울 때는 맞았다). 화면만 틀리고, 어디서 틀어졌는지 프리팹을 아무리 봐도 안 나온다 | **검사** — 프리팹 값을 굽는 것만으로 닫지 말고 **«재생 중»에 그 값을 되읽어 대조**한다. 주입 코드는 **자기가 쓰는 필드만** 건드리게 하고, 나머지를 덮어쓰지 않는지 검사에 넣는다 | Client · UI/UX |
| 71 | **«저장되지 않는» 프로퍼티에 값을 굽는다** — 어떤 프로퍼티는 대입하는 순간 **새 인스턴스를 만들어** 그 자리에만 살고, 프리팹·에셋에는 참조가 굳지 않는다. 빌더 로그는 성공이고 값도 들어갔는데 **런타임에는 기본값**이 뜬다 | **검사** — 구운 뒤 **파일(직렬화된 형태)에서 그 참조가 실제로 남았는지** 확인한다(#8 「생겼다」의 참조판). 인스턴스를 만드는 프로퍼티 대신 **공유 에셋을 만들어 물린다** | 아트 · Client |
| 72 | **판정용 캡처를 «낮은 해상도»로 찍는다** — 반해상도로는 「칸이 4개인가 5개인가」·「선이 하나인가 둘인가」가 **원리적으로 안 갈린다.** 그런데 캡처는 남아 있으니 「봤다」고 여기게 되고, 결함이 **눈으로 확인했다는 기록과 함께** 통과한다 | **체크리스트** — 세는 판정(개수·간격·경계)은 **기준 해상도 이상**으로 찍는다. 「눈으로 봤다」로 닫기 전에 **그 캡처에서 그 판정이 가능한지** 먼저 묻는다 | 연출 · UI/UX |

| 73 | **«난이도»를 옮기지 않고 «배치»만 옮긴다** — 배치·좌표·수치를 전수로 옮기면 「같은 레벨」이라고 여기게 된다. 그런데 플레이어가 느끼는 난이도는 **「성공하는 입력 구간의 폭과 개수」**다. **정답지가 «점 몇 개»뿐이면 그 구간을 한 번도 대조하지 않은 것**이고, 점이 다 맞아도 **구간이 넓거나 좁거나 구멍의 위치가 다르면 다른 게임**이 된다 | **정답지 + 검사** — 스테이지·레벨마다 **입력을 전 구간 스윕해 «성공 구간»을 재고**(하한·상한·폭·구간 개수), **이관본에 같은 스윕을 돌려 «구간 대 구간»으로 대조**한다. 합격 기준에 그 항목을 넣는다 — 없으면 아무도 안 잰다 | PD · Client · Design |
| 74 | **부분 구간만 훑고 «분포의 모양»을 결론낸다** — 좁은 자리 하나를 찾으면 「이 스테이지는 극도로 어렵다」로 적게 된다. 실제로는 **넓은 성공 구간이 따로 있고**, 잰 자리는 **구멍 안에 박힌 예외**였다. 반대로 성긴 격자로 훑으면 **넓은 구간이 파편화된 것처럼** 보인다 | **체크리스트** — 「좁다/파편화됐다」를 적기 전에 **입력 도메인 «전체»를 훑는다**(거친 스윕 → 세밀). 성공 사이에 낀 실패, 실패 사이에 낀 성공은 **각각 3회 이상 반복**해 구멍인지 «바늘»인지 가른다. ⚠ **폭이 입력 분해능에 가까운 것은 채점에 쓰지 않는다** — 곡선 고정용이다 | Client |

| 75 | **자동화가 «프로세스 이름»으로 프로그램을 죽인다** — 같은 엔진·에디터를 여러 프로젝트가 쓴다. 이름만 보고 정리하면 **사람이 다른 창에서 하던 저장 안 된 작업이 통째로 날아간다.** 우리 쪽 로그에는 「닫고 → 돌리고 → 열었다」로만 남아 **피해가 보이지도 않는다** | **도구** — 프로세스는 **«우리 것»이라는 근거**(명령줄의 프로젝트 경로 등)로 골라 죽인다. **못 가리면 죽이지 말고 «멈춘다»** — 남의 작업을 날리는 것보다 실패가 낫다. ⚠ **락파일 존재 ≠ 실행 중**(비정상 종료 시 낡은 락이 남는다) — 살아 있는 프로세스로 판정한다 | 전 파트 · PD |

| 76 | **«한 입력»에서만 재고 상수로 옮긴다** — 출발 위치·기준점·오프셋을 한 조건에서 재면 값이 하나로 나오고, 그걸 상수 컬럼에 박게 된다. 그런데 그 값이 **입력에 따라 변하는 «함수»**면(누르는 세기에 따라 발사점이 밀리는 식) **한쪽 끝에서만 맞고 반대쪽 끝에서 어긋난다.** 그리고 그 어긋남은 **평균을 보면 작아 보여** 오래 안 잡힌다 | **체크리스트** — 상수 컬럼에 넣기 전에 **입력을 «양 끝과 중간»으로 바꿔 가며 3점 이상** 재서 **값이 움직이는지** 본다. 움직이면 **함수로 옮기고**, 안 움직이면 「입력 3점에서 불변 확인」을 표에 적는다. ⚠ 여러 «스테이지»를 재는 것으로는 안 잡힌다 — **같은 스테이지에서 입력을 바꿔야** 드러난다 | Client · Design |

| 77 | **안전 장치를 «고치고» 그 장치가 실제로 발동하는지 안 태워 본다** — 문법 검사와 부분 시험만 하고 넘기면, 고친 코드가 **조용히 죽어 있어도** 모른다. 실제로 안전 검사를 추가하면서 **그 함수가 «아직 정의되지 않은 변수»를 쓰게** 만들어, 검사가 매번 실패하고 **판정이 언제나 거짓**이 됐다 — 「위험을 막았다」는 안심만 남고 **원래 있던 기능까지 통째로 죽었다** | **체크리스트** — 도구를 고쳤으면 **그 장치가 발동하는 경로를 «실제로 한 번 태운다»**(참이 나와야 할 상황과 거짓이 나와야 할 상황 둘 다). ⚠ 스크립트 «위쪽»에 함수를 추가할 때는 **그 함수가 쓰는 변수가 그 시점에 정의돼 있는지** 본다 — 엄격 모드에서는 조용히 죽는다 | 전 파트 · PD |

| 78 | **설정 «파일»을 보고 실효값이라고 단정한다** — 엔진 설정은 런타임 코드가 덮어쓰는 일이 흔하다. 파일에 적힌 기본값을 보고 「우리는 이렇게 돈다」로 결론내면 **없는 원인을 쫓고, 진짜 원인은 계속 남는다.** 실제로 「우리 물리는 50Hz」라는 판단이 파일만 본 것이었고 실효값은 **이미 원본과 같았다** | **체크리스트** — 설정은 **재생 중에 «되읽어»** 확인한다(엔진 API 로 실효값 덤프). 파일 값과 다르면 **덮어쓰는 코드를 찾아 적는다** — 어느 쪽이 진실인지가 문서에 남아야 다음 사람이 같은 데서 안 헤맨다 | Client · PD |
| 79 | **지문·불변식을 «통과한 표본»에서만 재고 전체 성질로 적는다** — 잘 맞는 샷들만 보고 「재접촉 0건 · 접촉 1~2프레임」 같은 지문을 세우면, **정작 갈리는 샷에서 그 지문이 크게 깨져 있어도 모른다.** 그러면 「지문은 맞는데 결과가 다르다」는 잘못된 그림이 남는다 | **체크리스트** — 지문·불변식은 **«실패한 표본»에서 반드시 다시 잰다.** 통과 표본과 실패 표본의 지문을 **나란히** 적어라 — 둘이 다르면 그 차이가 곧 단서다 | Client |

| 80 | **«A 뒤에 B 가 보였다»를 「A 가 B 를 만들었다」로 읽는다** — 충돌 뒤에 회전이 보이면 충돌이 회전을 만들었다고 읽고, 발사 시점 값을 0 으로 넣게 된다. 그러면 **첫 사건은 맞고 그 뒤가 갈리는** 진단하기 어려운 격차가 생긴다. 관찰을 더 해도 안 깨진다 — **선후가 인과처럼 보이기 때문**이다 | **체크리스트** — 인과를 세우기 전에 **«사이 구간»을 열거**한다(그 사이에 원인이라 주장한 사건이 **정말 있었는지** 전수로 센다). 실제로 「발사~첫 충돌 사이 접촉 0건」을 보이자 인과가 통째로 끊겼다. 그리고 **시작 시점의 값을 직접 재라** — 끝값에서 되풀지 말고 | Client · 전 파트 |
| 81 | **좌표계가 다른 엔진에서 «부호 있는 값»을 그대로 옮긴다** — 회전·각속도·y 방향처럼 축 방향에 의존하는 값은 원본과 대상의 좌표계가 다르면 **반대로 돈다.** 값의 크기가 맞으니 검사도 「값은 들어갔다」로 통과한다 | **체크리스트** — 부호 있는 값은 **추측하지 말고 «정답지로 판정»**한다(두 부호를 다 넣고 실측 정답지와 대조해 맞는 쪽을 고른다). 좌표계 차이는 **옮기는 지점 한 곳에서** 처리하고 주석에 남긴다 | Client · 아트 |

| 82 | **실측값을 넣었는데 «효과가 없다»고 되돌린다** — 원본에서 잰 설정을 넣었더니 점수가 안 오르거나 오히려 1~2점 나빠지면, 「효과 없는 변경」이라며 이전 값으로 돌리고 싶어진다. 그 순간 **점수가 값을 고르게 되고** 정답지는 검증이 아니라 «맞춘 것의 확인»이 된다. 게다가 그 설정은 **다른 상황에서 갈릴 수 있다**(이번엔 겹친 구속이 없어 효과가 없었을 뿐) | **체크리스트** — **실측값이면 효과와 무관하게 넣는다.** 대신 **대조군 수치를 문서에 그대로 박아**(A/B 각각의 점수) 다음 사람이 「왜 효과 없는 값을 넣었나」를 알게 한다. 되돌리는 근거는 **점수가 아니라 «새 실측»** 이어야 한다 | Client · PD |

| 83 | **원본이 결정적이라고 단정한다** — 같은 입력에 같은 답이 나오는 것이 정답지의 대전제인데, 웹 원본은 **프레임 간격이 고정이 아니라** 스침·저속 정착·다중 접촉 구간에서 결과가 갈린다. 「2회 돌려 같았다」로 결정적이라 선언하면, 나중에 안 맞는 자리를 전부 **우리 결함으로 오진**한다 (반대로 면죄부로도 오용된다) | **절차** — 정답지 후보는 같은 입력을 **n≥3 회** 돌려 갈리는지 본다. 갈리면 **지우지 말고 「비결정」으로 «분류»**하고 채점에서 빼되 **「비결정 N건 제외」를 리포트에 반드시 찍는다.** 골든은 **짧게 결판나는 샷**으로 고른다. ⚠ 제외 범위를 넓히는 것은 [#48] 과 같은 위반이다 | Client · PD |
| 84 | **자유 피팅이 「데이터마다 다르다」로 오독시킨다** — 파라미터를 다 풀어 놓고 항목별로 맞추면 항목마다 다른 값이 나오고, 「이건 레벨/개체 데이터구나」로 읽어 **표에 N벌을 적게** 된다. 실제로는 전역 상수 하나였고 범인은 **모델 범위 밖의 표본 한 점**이었다 | **절차** — 자유도를 **먼저 줄인다.** 이미 실측으로 아는 항은 **고정**하고 미지수 하나만 맞춘다. 「항목별로 다르다」는 결론은 **절편을 고정한 상태에서도** 갈릴 때만 인정한다. 자유도를 다 풀면 무엇이든 맞고, 맞았다는 사실이 정보를 잃는다 | Client |
| 85 | **상태가 바뀐 «뒤에» 읽어 판정한다** — 채록이 끝난 뒤 화면·레벨을 읽어 「성공했나」를 판정하면, 전이가 수 초 걸리는 원본에서 **성공을 놓친다**(실측: 4건 중 2건). 판정이 조용히 틀리므로 아래 모든 수치가 함께 틀어진다 | **절차** — 결과 판정은 **전이 결과**가 아니라 **그 순간의 플래그·변수**를 프레임마다 직독한다. 그러면 성공 «시각»까지 ms 로 나온다. 전이 지연이 있는지부터 재고 시작한다 | Client |
| 86 | **엔진이 내주는 값이 특정 구간에서 무효값을 낸다** — 접촉 법선 같은 파생값이 저속·깊은겹침 구간에서 `0` 벡터로 나온다(실측 19건 중 5건). 그 값을 무조건 믿는 채점기는 **그 구간을 통째로 오판**한다 | **체크리스트** — 엔진 파생값을 채점에 쓰기 전에 **유효성 검사**를 붙인다. 무효면 그 항목을 「측정 불가」로 남기고 **다른 축으로 채점**한다. 조용히 0 으로 계산하지 않는다 | Client |

| 87 | **문서 세트가 서로 모순된 채 오래 간다** — 확정표의 기본값이 「A」인데 규약 문서에는 「A 를 하지 않는다」가 단정형으로 남아 있었다. 담당자는 **자기가 먼저 읽은 쪽**을 따르므로 회차마다 결론이 갈리고, 갈렸다는 사실 자체가 안 보인다 | **체크리스트** — 규약 문서가 **선택지가 있는 항목을 단정형으로 적지 않는다.** 갈래가 있으면 **「확정표 N 이 가른다」**로 넘기고 **양쪽 길과 각각의 대가**를 적는다. md 를 고칠 때 **확정표에 같은 항목이 있는지** 먼저 본다 | PD |
| 88 | **정본 목록에서 빠진 문서는 아무도 안 쓴다** — 정답지 문서가 다른 문서에서 **경로로만 두 번 언급**될 뿐 산출물 목록·트리·규격·체크리스트 어디에도 없었다. 그래서 「무엇을 어떤 형식으로 적나」가 정해진 적이 없고, 회차마다 형식이 달라져 **표끼리 기준점이 섞였다** | **체크리스트** — 산출물을 늘리면 **목록·트리·규격 절·완료 체크리스트 «네 곳»을 같이** 고친다. 한 곳만 고치면 그 문서는 존재하지만 규격이 없다. **개수를 문장에 적지 않는다**(늘릴 때마다 낡는다) — 「표를 세어라」로 쓴다 | PD · 전원 |

| 89 | **극값(최고점·최저점·최대속도)을 프레임 샘플로 재면 «샘플링 위상»이 값에 섞인다** — 원본은 가변 프레임, 우리는 고정 스텝이라 **같은 궤적인데 다른 극값**이 나온다. 그 흔들림을 「원본이 비결정」으로 읽고 정답지를 통째로 버리게 된다. 실제로 12 px 흔들림의 정체가 **정확히 한 프레임 이동거리**였다 | **절차** — 극값은 **샘플 최대값이 아니라 «보간 꼭짓점»으로** 잡는다(양옆 표본으로 포물선 정점). 위상 독립이라 폭이 한 자릿수 줄고(실측 0.4 px → 0.05 px), **원본과 우리가 같은 정의로** 재게 된다. ⚠ **정답지가 흔들리면 원본을 의심하기 전에 «재는 법»부터 의심한다** | Client |
| 90 | **차이가 «한 방향으로만» 쏠렸는데 비결정으로 처리한다** — 「원본도 갈린다」는 가장 달콤한 결론이라, 우리가 못 하는 자리를 보고 원본을 두어 번 돌려 갈리는 것만 확인하면 바로 닫힌다. 실제로 등재 근거가 **n=1~2** 였고, 원본을 **124발** 다시 쏘니 **87 % 골인**인데 우리는 **전부 0** 이었다 — 계통 오차였다 | **판정 규칙** — **무작위는 양쪽으로 갈린다.** 차이가 전부 한 방향이면 그것은 비결정의 지문이 **아니라 계통 오차의 지문**이다. 비결정으로 빼려면 **성공률을 n≥10 으로 세서** 원본과 우리 숫자를 나란히 적는다. 원본 9/10 인데 우리 0/10 이면 **비결정과 무관한 우리 결함**이다 | Client · PD |

| 91 | **「닿았나」만 세면 «쌍이 아예 없었다»를 못 본다** — 엔진의 접촉 목록을 「닿은 것」으로 걸러서 보면, **② 닿을 수 있는데 안 닿았다** 와 **③ 애초에 쌍이 만들어지지 않았다** 가 **똑같이 「0건」으로** 보인다. 이 둘은 원인이 완전히 다른데 한 화면에서 구분이 안 되므로, 「우리 물리 계수가 틀렸나」를 몇 회차씩 뒤지게 된다 | **절차** — 접촉을 잴 때 **필터 전 원시 쌍 목록과 「닿은」 목록을 «둘 다»** 찍는다. 원시 쌍이 0 이면 계수 문제가 **아니라** 쌍 생성 단계(필터·레이어·콜백)의 문제다. **같은 프레임의 「정상 동작하는 다른 쌍」을 대조군으로** 같이 찍으면 「엔진이 죽은 것」과 갈린다 | Client |
| 92 | **필터 후보를 지목하고 «반례를 안 훑는다»** — 두 개체의 값이 다르면 곧바로 「이게 필터구나」가 된다. 실제로 값이 다른 필드를 범인으로 지목했는데, **정상 충돌하는 제3자가 같은 값**을 갖고 있어 반증됐다(그 필드는 충돌 «도형 선택»이었다) | **절차** — 후보를 지목하면 **그 값을 갖는 개체를 전수로 세어** 반례를 찾는다. 「A 는 1, B 는 2, 그래서 1↔2 가 안 부딪힌다」는 **1 을 가진 다른 개체가 B 와 부딪히는 순간 죽는다.** 전수를 세기 전에는 가설이지 실측이 아니다 | Client |

| 93 | **「지금 재생 중인 것」만 읽으면 다른 상태의 자산을 통째로 놓친다** — 개체의 현재 애니메이션·현재 프레임만 읽어서 「이 개체는 이게 전부」로 적는다. 특정 사건에서만 재생되는 애니메이션은 **평소에 존재조차 안 보이고**, 그래서 「관측 0회」로도 안 올라간다. 실측 사례: 골대가 평상시와 **다른 애니메이션**을 골인에만 쓰고 있었다 | **절차** — 개체를 훑을 때 **현재 것이 아니라 «컨테이너를 전수»로** 읽는다(애니메이션 목록 · 프레임 목록 · 상태 목록). 「이 개체에 몇 개가 있나」를 먼저 세고, 그 중 **관측한 것이 몇 개인지** 적는다. 세지 않으면 「없다」와 「아직 안 봤다」가 구분되지 않는다 | Fx · UIUX |
| 94 | **합성된 화면을 캡처해서 색을 잰다** — 화면에 보이는 색은 **원본 자산 색 × 런타임 착색**의 결과다. 그걸 자산 색으로 적으면 **자산을 잘못 굽고**, 더 나쁘게는 **「색이 여러 종류다」라는 틀린 전제의 질문**이 미측정 목록에 몇 회차씩 남는다. 실측 사례: 배경 텍스처의 불투명 색은 **흰색 1종뿐**이었고 색은 전부 런타임이 입히고 있었다 — 「프레임별 색값을 재라」는 항목 자체가 성립하지 않는 질문이었다 | **절차** — 색·두께·기하는 **소스 텍스처를 꺼내서** 잰다(엔진이 이미지 데이터를 캔버스로 뽑아 주는 경로가 대개 있다). 캡처는 **배치 확인용**이지 자산 측정용이 아니다. 자산이 단색 마스크로 나오면 **착색 «기전»을 옮긴다** — 결과 색을 여러 벌 굽지 않는다 (`#53`) | Fx · UIUX |
| 95 | **불연속 변화를 «트윈»으로 가정한다** — 원본이 한 프레임에 값을 «점프»시키고 그 뒤에 복귀 곡선을 태우는데, 이것을 하나의 트윈으로 보면 **시작값이 원리적으로 안 잡힌다.** 그러면 검사가 실행마다 흔들리고, 「첫 프레임 문제라 못 읽는다」는 주석을 달고 **허용치를 낮추고 싶어진다** | **절차** — 검사가 흔들리면 **허용치를 손대기 전에** 그 구간에서 **원본이 무엇을 하는지 rAF 마다** 다시 잰다. 점프면 **점프로 모델링**한다(시작값 1개 + 복귀 곡선). 흔들림은 측정 잡음이 아니라 **모델이 틀렸다는 신호**일 때가 있다 | Fx · Client |

| 96 | **무효값이 나오는 조건을 «같이 관측된 것»으로 적는다** — 엔진이 무효값을 내는 접촉을 모아 보니 전부 저속이고 전부 후반이었다. 그래서 「저속·깊은겹침·후반 전용」이라고 적었다. **전부 틀렸다** — 진짜 조건은 **상대가 «원 도형»인 것**이었고, 그 조건으로 다시 세니 예외 0건이었다. 틀린 조건을 적으면 **채점기가 엉뚱한 구간을 걸러내고**, 진짜 원인(그 도형이 우리에게 없다)이 **몇 회차씩 안 보인다** | **절차** — 무효값을 만나면 **판별자를 직독**한다(타입 필드 · 상대 개체 · 도형 종류). 「같이 관측된 성질」로 조건을 쓰지 않는다. 조건을 세웠으면 **그 조건으로 전수를 다시 갈라** 예외 0건을 보인다. ⚠ 무효값은 **「못 읽는 값」이 아니라 「다른 것과 부딪혔다」는 신호**일 수 있다 | Client |
| 97 | **원하는 입력값을 못 짚는데 «측정 축»을 안 바꾼다** — 대리 축(누른 시간 등)으로 목표값을 겨냥하면, 그 축과 실제 값 사이에 양자화·지터가 끼어 **같은 입력이 두세 값을 넘나든다.** 그러면 특정 값이 회차를 거듭해도 「미측정」으로 남고, 그 자리가 **정답지의 구멍**이 된다 | **절차** — **「못 짚는다」가 두 회차 반복되면 축을 의심한다.** 대리 축으로 겨냥하지 말고 **실제 값을 폴링하다 목표에 닿는 순간 입력을 끝낸다.** 실측 사례: 두 회차 실패하던 자리를 축을 바꾸자 **첫 발에 소수 4자리까지** 짚었다. ⚠ 되먹임 보정을 넣으면 **이상치 게이트가 필수**다 — 포화 표본 한 발이 보정을 밀어 뒤 시행을 통째로 망친다 | Client |

| 98 | **`[육안]` 으로 적어 둔 것을 나중에 정확값으로 «채운다»** — 초안에 「좌측면 분홍」처럼 **이름만** 적어 두고, 뒤 회차에 hex·수치로 채울 때 **원본을 다시 재지 않고 «문서 안에서 같은 이름을 찾아»** 옮긴다. 그러면 **이름이 같은 «다른 개체»의 값**이 들어앉는다(실측 사례: 「분홍」→ 컨페티 색 · 「하늘」→ 야자수 색). 화면이 그럴듯해서 **17회차를 살아남았다** | **절차** — `[육안]` 칸은 **반드시 원본을 다시 재서** 채운다. 문서 내 검색으로 채우지 않는다. 정확값으로 승격할 때 **① 그 이름을 가진 다른 개체가 문서에 있는지** 보고 **② 그 값이 원본 화면에 실제로 몇 픽셀 있는지** 센다 — 0 px 이면 옮겨 온 값이다. ⚠ **작은 요소일수록 위험하다** — 이웃에 덮여 몇 px 만 보이는 자리는 육안·최빈색 어느 방법으로도 못 집는다 | Design · UIUX |
| 99 | **「교차 검산 축」이라며 남긴 값을 «아무도 대조하지 않는다»** — 같은 사실이 두 곳(데이터 칸 / 굽는 상수)에 있는데 대조기가 없으면 **조용히 갈리고 화면은 안 변한다.** 「기록으로 남긴다」·「검산용이다」는 **대조가 실제로 도는 경우에만** 참이다 | **체크리스트** — 값을 「검산 축이라 남긴다」고 판정하면 **그 판정과 «같은 회차에» 기계 대조기를 붙인다.** 허용 오차를 명시하고(손실 압축 잡음 등) 굽는 도구 실행 끝에 **자동으로** 돌게 한다. 대조기를 못 붙이겠으면 **남길 근거가 없는 것**이다 | Client · PD |

| 100 | **원본을 재서 원인을 찾고는 «우리 쪽이 실제로 다른지»를 안 보고 반영 패스를 연다** — 원본에서 「X 는 이렇게 되어 있다」를 확증하면 곧바로 「그러니 우리는 X 가 틀렸겠다」로 넘어간다. 실제로 **우리 쪽은 이미 그 값과 소수점까지 같았고**, 담당자는 한 패스를 통째로 「이미 맞았음을 확인」하는 데 썼다. 더 나쁜 것은 **틀린 원인을 지목한 채 다음 축을 안 찾는 것**이다 | **체크리스트** — 반영 패스를 열기 전에 **「우리가 지금 무엇으로 되어 있나」를 먼저 읽는다**(한 줄 조회로 끝난다). 지시문에 **「우리 쪽이 무엇인지 먼저 확인하고 보고하라」**를 넣고, **다르다는 것이 확인된 뒤에** 고치게 한다. ⚠ 이건 담당자가 아니라 **지시한 쪽(PD)의 결함**이다 | PD |

| 101 | **우리 거동을 「원본에 없는 것」으로 단정하는데, 원본의 «실패 분기»를 안 봤다** — 원본에서 **성공한 시행만** 채록해 놓고 「원본은 이렇게 안 한다」고 결론 낸다. 그런데 우리 거동이 사실 **원본이 실패할 때 하는 바로 그 거동**이면, 우리는 「원본에 없는 버그」를 몇 회차씩 쫓게 된다. 실측 사례: 「원본 공은 정착하지 않는다」고 적었는데 원본도 **698 프레임을 머물고 잠들었다** — 다만 **미골인 시행에서만** 그랬다 | **절차** — 「원본은 X 를 안 한다」를 적기 전에 **원본에서 «우리와 같은 결과»가 난 시행을 찾아** 대조한다. 성공 샷하고만 비교하면 **분기 자체를 못 본다.** 그러면 질문이 바뀐다 — 「왜 우리는 X 를 하나」가 아니라 **「어디서 성공 분기와 갈라졌나」**가 되고, 그 자리는 대개 **훨씬 앞의 접촉 한 번**이다 | Client · PD |

| 102 | **«물리가 앉힌 자리»를 레벨 데이터로 N벌 적어 둔다** — 개체의 최종 위치를 레벨마다 관측해 표에 박는다. 그런데 그 값이 사실 **다른 개체에서 유도되는 값**이면(바닥에 얹혀 물리가 정착시킨 자리 등), **못 잰 레벨에서 반올림·추정으로 채우게 되고** 그 칸만 조용히 틀린다. 실측 사례: 발사대 y 5벌 중 하나를 유도 반올림으로 채워 **0.241 px** 어긋나 있었다 | **판별법** — 값이 **다른 값과 «일정한 차»로 묶이는지** 본다. N 벌 전부에서 잔차가 0 이면 **데이터가 아니라 유도값**이다. 그러면 표에서 빼고 **기전을 옮긴다**(`#53`) — 못 잰 레벨도 자동으로 맞고, 그 차의 정체(반높이 · 스킨 · 슬롭)가 **다른 계산의 근거**가 된다 | Client · Design |
| 103 | **「읽었더니 전부 0」을 «값»으로 받아들인다** — 화면·버퍼에서 픽셀을 읽었는데 전부 검정이면 「검은 화면」이 아니라 **「읽을 수 없는 것」**일 때가 있다(가속 캔버스가 버퍼를 보존하지 않는 설정 등). 0 을 값으로 적으면 **색·대비 판정이 통째로 조용히 틀린다** | **체크리스트** — 픽셀을 읽기 전에 **눈에 보이는 색이 있는 영역**으로 경로를 한 번 검증한다. 전부 0 이 나오면 **읽기 경로가 죽은 것**으로 보고 다른 경로(스크린샷 등)로 바꾼다. 일반 규칙 — **「전부 같은 값」은 측정 결과가 아니라 «측정이 안 됐다»는 신호**로 먼저 의심한다 | Fx · UIUX |

| 104 | **여러 줄 출력을 한 덩어리로 뭉개 놓고 「하나」로 다룬다** — 프로세스 목록처럼 **여러 줄이 나올 수 있는 출력**에서 개행까지 함께 제거하면 `22100` + `24432` 가 **`2210024432`** 라는 없는 값이 된다. 숫자 형식 검사도 통과하므로 **조용히 아무 대상에도 안 맞고**, 「했다」고 믿은 채 다음 단계가 실패한다. 로그에는 실패 흔적조차 안 남는다 | **체크리스트** — 캐리지리턴만 지운다(`tr -d '
'`). 개행은 **구분자라서 지우면 안 된다.** 그리고 **「하나일 것」이라고 가정하지 않는다** — 엔진 프로세스는 에디터 + 워커 N개처럼 여럿일 수 있다. 목록을 다루는 함수는 **몇 개를 찾았는지 로그로 찍게** 해서 0개·N개가 눈에 보이게 한다 | 전원 |

| 105 | **실측 소수에 «맞춘» 상수를 만든다** — 관측값이 `0.7495` 로 나오면 그 숫자를 그대로 상수로 박는다. 그런데 그 값이 사실 **구성 요소로 유도되는 값**(`2×스킨 − 슬롭 = 0.75`)이면, 박아 둔 상수는 **그 회차의 관측 잡음까지 굳힌 것**이라 다른 상황에서 어긋나고 **왜 그 값인지 아무도 설명할 수 없다** | **절차** — 상수를 박기 전에 **이미 아는 값들로 «유도되는가»**를 본다. 유도값과 관측값의 차가 **측정 정밀도 안**이면(실측 사례: 잔차 0.0005 px) **유도식을 쓴다.** 관측 소수를 그대로 쓰는 것은 «맞춘 것»이지 «안 것»이 아니다 (`#48` 과 같은 위반) | Client · Design |
| 106 | **검사기 둘이 같은 입력에 다른 답을 내는데 점수부터 읽는다** — 채점기가 여럿이면 같은 시행을 서로 다르게 재현할 수 있다(한쪽이 실제 경로를 안 지나거나 초기화가 다르다). 이걸 두고 다른 숫자를 계속 보면 **그 숫자 전부의 근거가 흔들린다** — 어느 쪽이 맞는지 모르는 채 개선·악화를 논하게 된다 | **게이트** — **같은 입력에 두 검사기의 결과가 갈리면 그것부터 닫는다.** 점수 보고보다 우선이다. 닫는 법은 **둘의 실행 경로를 나란히 찍어** 어디서 갈라지는지 보는 것이다(초기화 · 스텝 수 · 주입 지점). 못 닫으면 **두 숫자를 «둘 다» 보고**하고 어느 쪽도 정본으로 쓰지 않는다 | Client · PD |
| 107 | **「엔진이 언제 무엇을 했나」를 «관찰»로 답하려 한다** — 위치·속도 시계열을 아무리 촘촘히 떠도 「한 스텝 차이인 것 같다」까지밖에 못 간다. 그 상태로 반영하면 **가설을 코드에 박는 것**이고, 나중에 틀렸을 때 되짚을 근거가 없다 | **기법** — 순서·횟수는 **함수를 후킹해서 직독**한다(엔진 메서드를 감싸 호출 시각·인자·횟수를 기록). 실측 사례: 「임펄스 뒤 스텝 0회 · 다음 틱에 1회」가 **17/17 예외 0** 으로 닫혔고, 스텝 인자가 그 프레임 dt 그대로임(누적기 없음)까지 같이 나왔다. **관찰은 「무엇이 보이나」, 후킹은 「무엇을 했나」** — 둘은 다른 질문이다 | Client |
| 108 | **가설을 «죽일 조건»을 안 정하고 확증만 쌓는다** — 가설에 맞는 관측을 모으면 언제나 그럴듯해진다. 그러면 **틀린 가설이 회차를 넘어 살아남고**, 그 위에 쌓은 반영이 전부 헛돈다 | **절차** — 재기 «전에» **「이 값이 이러면 가설은 죽는다」를 써 놓고** 시작한다. 실측 사례: 「원본 자체의 산포가 8 ms 를 넘으면 위상 가설은 죽는다」를 먼저 적고 쟀다 — 산포는 ≤ 1.3 ms 였고 **가설이 «견뎠다».** 살아남은 가설과 **한 번도 시험받지 않은 가설**은 무게가 다르다. ⚠ 구별을 위해 **살아남은 근거를 「무엇이 아니었나」로도** 적는다(부호·빈도·지속이 우리 것과 어떻게 달랐나) | 전원 |
| 109 | **물리 엔진 위에서 도는 검사기를 «오브젝트만 헐고 다시 세워» 재사용한다** — 빨라서 그렇게 하게 된다. 그런데 엔진은 지운 바디의 내부 자리를 **자유목록에서 역순으로 재사용**하므로 바디 «순서»가 바뀌고, 솔버가 임펄스를 **더하는 순서**가 바뀌어(부동소수 비결합) 그 1e−7 이 증폭된다 ⇒ **같은 입력이 다른 답을 낸다.** 더 나쁜 것은 **모든 시행이 아니라 «미세한 스침이 있는 시행만» 흔들린다**는 것이라 「대개 재현된다」로 보이고, 그 흔들림을 **게임 결함으로 오독**하게 된다 | **게이트 + 도구** — 채점 전에 **«같은 입력을 연속 N회»** 쏴서 결과가 같은지 먼저 본다(갈리면 **첫 갈림 스텝**을 찍는다 — 경로를 나란히 놓는 #106 의 구체 방법이다). 갈리면 **시뮬레이션 «월드»를 시행마다 통째로 새로 만든다** — 오브젝트만 허는 것으로는 부족하다. ⚠ 이건 **원본 절차와도 같다** — 원본도 시행마다 판을 다시 깔기 때문이다 | Client · PD |
| 110 | **원본과 우리가 «언제 읽었는가»를 달리 해 놓고 시각(ms)으로 대조한다** — 원본은 보통 **스텝이 끝난 뒤 프레임**에서 사건을 읽는데, 우리 채점기는 **스텝 전** 시각을 적기 쉽다. 그러면 **전 구간이 정확히 «한 스텝»만큼 한쪽으로 치우치고** — 부호가 한쪽이고 사슬 끝까지 실려 나가서 **«계통 오차 = 물리 결함»으로 오독**된다. 실제로 한 회차가 «이미 맞은 위상»을 고치러 갈 뻔했다 | **체크리스트** — 사건 대조는 **시각이 아니라 «스텝/프레임 수»로** 먼저 맞춘다 — «우리 적분 횟수»와 «원본 프레임 수»가 같으면 위상은 맞은 것이고, 남은 차는 «규약»이다. 반영 패스를 열기 전에 **우리가 지금 어느 시점을 적고 있는지를 먼저 읽는다**(#100) | Client · PD |

| 111 | **음성 대조를 «아무 대조군»으로 만든다** — 「A 를 넣으니 좋아졌다」를 확인할 때 A 를 그냥 빼 보면, 「A 의 «내용»이 맞아서」인지 「A 처럼 «무언가 흔들어서»」인지 못 가른다. 그러면 우연히 맞은 값을 원인으로 등재하게 된다 | **실험 설계** — 대조군을 **「통계는 같고 «순서/구조»만 다른 것」**으로 만든다. 실측 사례: 원본의 실제 프레임 간격 시퀀스를 먹이니 어긋남이 닫혔는데, **같은 값들을 순서만 뒤집은 대조군**은 닫지 못하고 오히려 다른 자리를 깨뜨렸다 — 평균·분산·합이 같으므로 **「그 실제 순서」가 원인**임이 갈렸다. 「빼 보기」는 약한 대조, **「섞어 보기」가 강한 대조**다 | 전원 |
| 112 | **정답지 값을 «도달 불가능한 자리»에 잡아 둔다** — 입력이 프레임마다 누적되는 방식이면(누른 시간 × 증가율) **도달 가능한 값이 이산 격자**가 되고, 그 격자는 **입력 시작의 프레임 위상**에 물려 조금씩 움직인다. 격자 사이의 값을 정답지에 넣어 두면 **원본이 그 값을 낼 수 없어** 영원히 「미측정」으로 남고, 우리 쪽이 맞는지 틀리는지도 판정할 수 없다 | **절차** — 정답지 값을 정하기 전에 **도달 가능한 격자를 실측**한다(입력 중 실제 값을 전 표본 채록 → 시작점·간격 산출). 격자 간격보다 **드리프트 폭이 작으면 그 값은 원리적으로 못 낸다** — 「미측정」이 아니라 **「도달 불가」로 분류**하고 **격자 위의 값으로 정답지를 옮긴다** | Client |

| 113 | **해소된 「미측정」에 아무도 취소선을 안 긋는다** — 뒤 회차가 실제로 재서 값이 나왔는데, **그 값을 적은 문서와 미측정 목록이 달라** 목록은 그대로 남는다. 회차가 쌓이면 목록이 부풀고, **진짜 구멍이 낡은 항목 사이에 묻힌다.** 그러면 「미측정이 비었나」로 완료를 판정할 수 없게 되고, 마감이 통째로 막힌다 | **절차** — 값을 잰 회차가 **그 자리에서 원 목록의 항목에 취소선 + 「해소 (N회차)」 + 값이 있는 절 번호**를 적는다. 마감 전에는 **전수 훑기**로 「해소 / 원리 불가 / 범위 밖 / 열림」 4갈래로 다시 가른다. ⚠ **「아마 해소됐을 것」으로 닫지 않는다** — **값의 «자리»를 확인한 것만** 해소다. **낡은 미측정 목록은 없는 것보다 나쁘다** | 전원 · PD |

| 114 | **배치표를 «값»부터 대조한다** — 좌표가 조금 어긋나면 「반올림이겠지」로 넘어가는데, 실제로는 **그 표가 다른 것을 적은 것**일 때가 있다. 실측 사례: 장식 한 줄이 world 좌표·**개수(2 vs 5)**·정규화좌표·표시크기가 **전부** 안 맞았다 — 값을 고치는 문제가 아니라 **줄 자체가 틀린 것**이었고, 그대로 이관하면 그 장식이 엉뚱한 자리·크기로 간다 | **절차** — 배치표는 **«개수»부터 맞댄다.** 개수가 다르면 값 대조는 의미가 없다 — 그 표는 **다른 대상을 적은 것**이다. 개수 → bbox 합집합 → 개별 좌표 순으로 좁힌다. 렌더 일치율이 낮은데 「아트 품질 탓」으로 읽히면 **배치표 개수부터** 본다 | UIUX · Fx |
| 115 | **인스턴스가 없으면 그 자산은 «클래스에도» 안 보인다** — 특정 사건에만 생성되는 개체는 평소에 인스턴스가 0 이고, 그러면 **클래스의 자산 목록조차 비어 있다.** 「목록을 전수로 읽는다」(#93)만으로도 못 잡는다 — 목록 자체가 없기 때문이다 | **기법** — 그 사건이 나는 프레임에 **훅을 걸어 참조를 붙잡아 두고**, 개체가 사라진 «뒤에» 그 참조로 자산을 꺼낸다. 사건을 한 번 일으키는 것이 유일한 관측 기회라는 뜻이다 — **사건 목록을 먼저 만들고**(골 · 실패 · 전환 · 최초 진입) 각각을 한 번씩 일으켜 훑는다 | Fx |

| 116 | **「정본 문서가 틀렸다」를 «이관물이 틀렸다»로 옮겨 적는다** — 학습 담당자는 **원본 ↔ 문서**를 대조한다. 그 보고를 받은 쪽(PD)이 그것을 **원본 ↔ 이관물**의 결함으로 바꿔 지시하면, 이관 담당자는 **이미 맞아 있는 것을 고치러** 간다. 실측 사례: 배치표 한 줄이 틀렸다는 보고가 「우리 배치가 틀렸다」로 전달됐는데, **이관본은 5/5 맞아 있었고** 틀린 것은 문서 줄(다른 화면의 값이 섞여 들어간 것)뿐이었다 — 한 패스를 통째로 확인에 썼다 | **체크리스트** — 학습 보고를 지시로 바꿀 때 **「무엇과 무엇을 대조한 결과인가」**를 먼저 확인한다. **문서 결함**과 **이관 결함**은 다른 일이고 담당도 다르다. 지시문에는 항상 **「우리 쪽이 지금 무엇인지 먼저 읽고 보고하라」**를 넣는다(`#100`) — 그러면 전제가 틀려도 한 줄 조회로 끝난다 | PD |

| 117 | **마스크·형상을 «요약 통계»로 보고 모양을 단정한다** — 알파를 반경별로 평균 내거나 히스토그램만 보면 **찬 원과 링이 똑같이 보인다.** 그렇게 적은 서술로 자산을 구우면 모양이 아예 다른 것이 나오고, 픽셀 대조를 하기 전까지 안 잡힌다. 실측 사례: 「6프레임 링」으로 적힌 것이 실제로는 **찬 원 → 마름모 → 십자 → 끝동강 → 무** 였다 | **절차** — 형상은 **«그려서» 확인한다**(마스크를 아스키/이미지로 덤프). 요약값은 **크기·개수·알파처럼 스칼라인 것**에만 쓴다. 그리고 구운 결과를 **소스 마스크와 픽셀 대조(IoU)** 해서 닫는다 — 실측 사례에서 픽셀 중심 규약(`+0.5`)을 빼먹은 것이 **IoU 0.981 → 0.936** 으로 드러났다 | Fx · UIUX |

| 118 | **「틀렸다」고 «표시»하는 데는 실측을 안 쓴다** — 값을 고칠 때는 근거를 요구하면서, **취소선을 긋고 「이 줄은 틀렸다」고 다는 것**은 심증만으로 한다. 그러면 **맞는 값이 틀린 것으로 낙인**찍히고, 뒤 회차가 그 표시를 믿고 다시 재지 않는다. 실측 사례: 정상 값 세 줄에 취소선이 달렸고, 그 자리를 유도값으로 메웠는데 **원래 값이 맞고 유도값이 틀렸다** | **체크리스트** — **취소선은 정정만큼 무겁다.** 「틀렸다」를 달려면 **그 자리를 «다시 잰» 근거**가 있어야 한다. 없으면 「의심 — 재측정 대기」로 적는다. ⚠ 특히 **원인을 「엔진의 알려진 함정」으로 설명할 때** 조심한다 — 그 함정이 **그 자리에는 적용되지 않을 수 있다**(실측 사례에서 그 화면은 깊이가 0이라 함정이 안 걸렸다) | 전원 |
| 119 | **전 프레임 채록을 «필터 없이» 돌려 도구가 죽는다** — 촘촘히 뜨는 것이 좋다고 배웠으니 조건 없이 전부 덤프하게 되는데, 개체가 많은 화면에서는 **메모리 한계에 부딪혀 프로세스가 죽고 그 회차 채록이 통째로 유실**된다. 그런데 죽기 전까지는 잘 도는 것처럼 보인다 | **체크리스트** — 채록 전에 **한 프레임의 덤프 크기 × 예상 프레임 수**를 어림한다. **얕은 덤퍼**(필요한 필드만) + **관심 개체만** 거른다. 긴 구간은 **주기적으로 파일로 흘려보내고** 메모리에 쌓지 않는다. 죽으면 **부분 결과라도 남게** 중간 저장을 건다 | 전원 |

| 120 | **「미측정」 목록만 훑고 «옮기다 만 것»은 못 본다** — 두 목록은 다른 것이다. **미측정 = 「잰 적 없는 값」**, **이관 공백 = 「잰 값을 안 옮긴 것」**. 미측정을 전수로 정리해 A 등급 0 을 확인해도, **원본에 있는 요소가 우리 화면에서 통째로 빠진 것**은 그 목록에 아예 안 뜬다. 실측 사례: 시작 화면 배경 장식 5개가 통째로 없었는데 미측정 185건 전수 훑기에서도 안 나왔다 | **게이트** — 마감 전에 **「원본 요소 → 우리 요소」 전수 대조표**를 «화면마다» 만든다(`UIUX` 「화면 하나를 전수 대조표로 연다」). 빠진 것은 **미측정이 아니라 «이관 공백»**으로, 등재 없이 다른 것은 **결함**이다. ⚠ 「의도된 차이」에 없는 차이는 전부 결함 — **미측정 목록이 비었다고 마감하지 않는다** | PD · UIUX |

| 121 | **«표본»에 의존하는 검사를 그대로 두고 마감한다** — 재생 중에 프레임을 떠서 「6프레임이 다 보였나」를 세는 검사는, 프레임 지터로 **표본 하나를 놓치면 실패**한다. 그러면 실행마다 점수가 갈리고 «회귀»와 «표본 누락»이 구분되지 않는다. 그 상태로 보고하면 다음 사람이 **없는 회귀를 쫓는다** | **절차** — 표본 의존 검사는 **여러 번 관측해 합집합**을 쓰거나, **「표본을 놓쳤다」는 사실을 실패와 «다른 줄»로** 찍는다. ⚠ **허용치를 낮추는 것은 대책이 아니다**(`#48`). 마감 전에는 **전 검사를 두 번 돌려 같은 답이 나오는지** 확인한다 — 갈리면 그것부터 닫는다(`#106`) | Client · PD |

| 122 | **무작위 파라미터를 «관측 표본»으로 유도한다** — 무작위가 섞인 값(생성 위치·간격·속도)은 표본을 모아 최소·최대를 재게 되는데, 표본 몇십 개로는 **경계도 분포 모양도 못 정한다.** 그렇게 박은 값은 「맞춘 값」이라 다른 상황에서 어긋나고, 무작위라 **렌더 대조로도 검증이 안 된다** | **기법** — 엔진이 파라미터를 «식»으로 들고 있으면 **그 식을 반복 호출해 경계를 직독**한다(실측 사례: 4,000회 호출로 저작 상수가 통째로 나왔다). ⚠ **`distinct` 개수를 같이 세라** — 값이 −100~+100 에 흩어져도 **서로 다른 값이 2개뿐이면 «둘 중 하나 고르기»**이지 균등 난수가 아니다. min/max 만 보면 반드시 오독한다(`#74`) | Client · Fx |

| 123 | **분석과 이관 사이에 «분해»가 없어 이관이 뒤죽박죽이 된다** — 게임 전체를 한 덩어리로 놓고 「패스 ① 를 다 하고 ② 를 다 하는」 식으로 돌면, **② 에서 나온 결함이 ① 로 되돌아가고** 그 되돌아감이 회차마다 반복된다. 안에서는 진전이 보여도 **밖에서 보는 사람은 「지금 무엇을 만들고 있나」를 알 수 없고**, 「어디까지 끝났나」도 말할 수 없다 | **게이트 (G0-b)** — 학습이 끝나면 **작업 분해표**를 만든다. 단위는 **학습 산출물에서 기계적으로** 뽑고(시스템 문서 한 장 · 화면 하나 · 테이블 묶음 · 연출 덩어리 · 리소스군), **순서는 「의존 → 한 판 돌아가는 최소 경로 → 못 잰 것이 많은 것 → 작은 것」**으로 매긴다. ★ **패스 ①②③ 은 «한 단위 안에서» 도는 순서다** — 한 단위를 **검사까지 완주**하고 다음으로 간다. **끝난 단위를 다시 열었으면 「완료 조건이 약했다」는 뜻**이니 조건을 고쳐 표에 반영한다. 보고는 **`▶ <단위> 들어갑니다` / `✔ <단위> 완료 — <검사> N/N`** | PD |

| 124 | **재사용 문서에 «특정 도구 이름»을 박는다** — 그때 쓰던 도구의 함수명을 절차에 그대로 적으면, 그 도구가 없는 환경에서 담당자가 **없는 것을 호출하고 막힌다.** 문서는 「맞는 절차」로 보이므로 **틀렸다는 신호가 안 나온다** — 실제로 17곳이 그 상태로 여러 회차를 살아남았고, 담당자들은 절차서가 아니라 **프로젝트 설정 파일에서** 실제 수단을 배우고 있었다 | **체크리스트** — 절차에는 **«무엇을 하는가»**를 적는다(`[재생 멈춤]` · `[콘솔 읽기]`). **«어떤 도구인가»는 그 프로젝트의 「도구 제약」 표**가 정본이다. 도구 이름·함수 시그니처가 재사용 문서에 보이면 **그 자체가 결함**이다 | 전원 |

| 125 | **저장소 하나에 게임이 여럿인데 «프로젝트 설정은 하나»다** — 확정표도 맞고 그것을 설정에 넣는 코드도 있는데, **빌드가 그 코드를 안 부른다.** 그러면 **마지막에 만진 게임의 설정이 그대로 나간다** — 방향·패키지 이름·시작 씬이 전부. 에디터에서는 잘 돌기 때문에 **구워서 기기에 올리기 전까지 아무도 모른다** | **도구** — **게임별 빌드 진입점**을 만든다. ① 그 게임의 확정표를 **먼저** 적용 ② 씬을 **그 게임 것만** 남김 ③ **엔진에 되물어** 확인(`#78`) ④ 빌드. 그리고 **사람은 Build 버튼을 누르므로** 빌드 전처리에서 **시작 씬 경로로 게임을 알아내** 같은 것을 적용한다. ⚠ 알 수 없는 씬이면 **고치지 말고 경고만** — 임의로 정하는 것이 더 위험하다 | Transfer_Programmer · PD |

| 126 | **첫 화면이 enum «0번 값»이라 첫 전이 이벤트가 안 뜬다** — 화면 전이를 한 곳(플로우 객체)으로 모으면 「같은 화면으로의 재진입은 무시」가 들어간다. 그런데 현재 화면의 초기값은 **enum 의 0번 값**이고, 첫 화면은 대개 그 0번(로비·대기·타이틀)이다 ⇒ **첫 전이가 「바뀐 게 없다」로 되돌아 나가** 그 화면의 창만 통째로 안 열린다. ⚠ 현재 화면 값은 **맞게 보이고** 나머지 전이는 전부 정상이라, 단서가 「창 하나가 없다」뿐이다 | **코드 (공용 플로우)** — 「한 번이라도 들어갔는가」 플래그를 두고 **첫 진입은 같아도 알린다.** ⚠ **enum 순서를 바꿔 피하지 않는다** — 화면 목록은 원본 플로우 그대로여야 한다(0번 자리를 비우는 것도 원본에 없는 화면을 만드는 것이다). **게이트** — 플로우 diff 의 첫 줄은 **「첫 화면의 창이 열려 있는가」**로 시작한다. 현재 화면 값만 보는 검사는 이 결함을 통과시킨다 | Transfer_Programmer · PD |

| 127 | **「코드가 없는 원본」이라고 «단정»한다** — URL 만 받으면 블랙박스로 보고 «관측»만으로 규칙을 짜게 된다. 그런데 웹 게임은 **번들 소스가 브라우저로 내려온다** — 난독화돼 있어도 **저작 상수·식·클래스 구조는 문자열 그대로 남는다**. 실측 회차를 8번 돌고도 못 닫은 미측정이, 소스를 받자 **한 번에** 닫혔다 | **절차 (윗단 가지에 추가)** — URL 제공형이면 **①** 게임 프레임의 origin 에서 로드된 스크립트 URL 을 모으고 **②** 그 프레임 안에서 `fetch` 해 소스를 **로컬에 저장**한 뒤 **③** 오프라인으로 캔다. ⚠ **앵커를 먼저 정한다** — 이미 관측한 값(레벨 목표 · 간격)을 정규식으로 찾아 «그 근처»를 읽는다. 앵커 없이 그럴듯한 숫자를 줍는 것은 실측이 아니다. ⚠ **소스를 «베끼지» 않는다** — 규칙을 읽어 우리 구조로 다시 구현한다(아트는 여전히 재제작) | 전원 · PD |
| 128 | **실측이 잘 맞았다고 «규칙도 맞았다»고 믿는다** — 상수는 관측으로 2% 안에 맞출 수 있다(속도 169.3 vs 저작 170 · 간격 0.3576 vs 0.350). 그래서 「우리 수치가 맞으니 시스템도 비슷하겠지」로 넘어간다. 그런데 **시스템은 관측으로 못 본다** — 실측 사례에서 스폰이 「고정 간격 + 가중치 뽑기」가 아니라 **«위협 예산»**이었고, 조준이 「항상 최근접」이 아니라 **«마지막에 맞힌 적 유지»**였으며, 업그레이드가 승산이 아니라 **덧셈 누적**이었다. 셋 다 화면으로는 구분이 안 된다 | **체크리스트** — 값과 «규칙»을 다른 등급으로 관리한다. **값**은 실측으로 닫히지만 **규칙**은 ① 소스 직독 ② 함수 후킹(`#107`) ③ 죽일 조건을 건 가설 시험(`#108`) 중 하나가 있어야 닫힌다. ⚠ 규칙이 미측정인 채로 통과한 골든 벡터는 **「값만 맞은 것」**이다 — 그 사실을 판정에 적는다 | Client · PD |
| 129 | **자산을 «추가»했는데 로드 «주소»가 안 붙는다** — 그림을 굽고 임포터 설정까지 붙였는데 어드레서블 등록이 빠지면, 로드가 조용히 `null` 이 되고 렌더러는 **아무 말 없이 안 그린다**. 실측 사례: 시뮬은 「적 27마리」라고 했고 **화면엔 0마리**였다. 게다가 풀의 «대표 세트»가 그 빠진 자산이라 **다른 종류까지 통째로** 안 그려졌다 | **도구 + 게이트** — ① 등록을 **도구로** 만든다(폴더를 훑어 주소 = 파일 이름). ② **양방향 감사**를 붙인다 — 표에 있는데 주소가 없으면 오류, 주소가 있는데 표에 없으면 경고. ③ 로드 결과가 `null` 이면 **시끄럽게** 찍는다(조용한 `null` 이 이 사고의 본체다). ④ 「시뮬 개체 수 vs 켜진 렌더러 수」를 **캡처 로그에 같이** 찍는다 — 갈리는 순간 눈에 보인다 | Transfer_Artist · Transfer_Programmer |
| 130 | **「안 보였다」를 「없다」로 적는다** — 관측은 «그때 화면에 있던 것»만 준다. 조건부로 꺼지는 것은 영원히 안 보인다. 실측 사례 셋: **업그레이드 8종 중 4종**이 「해금 전 가중치 0」이라 안 떴고(우리는 4종짜리 표를 만들었다), **체력바**는 가득 차면 페이드아웃이라 「체력 UI 가 없다」로 적었고, **거리 표시**는 있는데 아무도 안 셌다 | **체크리스트** — 「없다」는 **「그 조건에서 안 나온다」로만** 적는다. 목록성 데이터(카드 풀 · 적 종류 · 화면)는 **개수를 «원본 정의»에서** 확인한다 — 관측으로 센 개수는 **하한**이다. ⚠ **화면의 문구를 전수로 나열**하는 것만으로도 빠진 요소가 드러난다(`#120` 의 값싼 실행 방법) | 전원 |

| 131 | **표의 주소·키 컬럼을 «읽는 곳»이 없다** — 아이콘 주소가 축마다 표에 제대로 들어 있는데 **그것을 스프라이트로 넣는 줄이 없었다.** 데이터 검사(행 수·Validate·조회)와 플로우 검사가 전부 통과했다 — **넣는 쪽이 없으면 어떤 «데이터» 검사도 안 걸린다** | **체크리스트** — 표의 주소/키 컬럼마다 **「이 값을 읽는 곳이 어디인가」**를 한 줄 적는다. 읽는 곳이 없으면 그 컬럼은 **아직 안 옮긴 것**이다. ⚠ 이 종류는 **캡처를 «눈으로 보는»** 것으로만 잡힌다 — 세 장이 같은 그림이라는 것은 숫자로 안 나온다. 마감 캡처는 사람이 한 번 본다 | Transfer_Programmer · Transfer_Artist |
| 132 | **원본지도에 적어 둔 함정을 다음 프로브가 그대로 밟는다** — 「모든 모달이 항상 씬에 있고 가시성으로만 켜진다」를 첫 회차에 적었는데, 뒤 회차의 새 프로브가 가시성 필터를 빼먹어 **숨은 창을 «떠 있는 것»으로 읽고** 첫 표본에서 끝났다 — 표본 0. 「함정을 적어 뒀다」와 「도구가 그것을 막는다」는 **다른 말**이다 | **도구** — 씬 그래프를 훑는 프로브는 **가시성 판정(부모 사슬의 visible·alpha)을 «공통 조각» 하나**로 두고, 텍스트·스프라이트 어느 쪽을 읽든 그것을 지난다. 원본지도의 함정 항목마다 **「어느 공통 조각이 막나」**를 적는다 — 비어 있으면 아직 함정이다 | 전원 (학습) |
| 133 | **규칙을 «추가»하자 검사 봇이 죽어 검사가 낡았다** — 접촉 피해를 넣은 순간 원을 그리며 돌던 봇이 레벨업 전에 죽어 「레벨업 도달 실패」로 찍혔다. **게임이 아니라 검사가 틀린 것**인데, FAIL 은 게임을 의심하게 만든다 | **체크리스트** — 규칙을 더한 회차에는 **그 규칙 아래서 봇이 아직 목표에 도달하는지**를 먼저 본다. 봇은 **원본 실측에 쓴 봇과 같은 규칙**(위험을 피하고 안전할 때 줍는다)으로 맞춘다 — 두 쪽이 같은 봇이어야 A/B 가 성립한다. ⚠ **봇은 «게임을 멈추는 창»을 사람처럼 치워야 한다** — 레벨 업·부활처럼 정지 게이트가 걸리는 창을 안 누르면 시뮬이 한 틱도 안 돌고, 봇은 계속 입력을 내지만 화면은 제자리다. 겉으로는 **「봇이 목표에 못 갔다」**로 보여 게임을 의심하게 된다. **실패했을 때 «정지 여부·거리·체력»을 같이 찍어** 두면 한 번에 갈린다 | Transfer_Programmer |
| 134 | **관측 «하한»을 값으로 적는다** — 체력을 「피격 6회」로, 무적을 「0.95초」로 확정했는데 소스는 **7 · 1.0초**였다. 마지막 타격은 **사망 화면에 가려** 안 보였고, 무적은 «연속 피격 간격의 최솟값»이라 **원리적으로 하한**이었다. 표본 3판이 전부 같은 값을 내서 의심할 이유가 없었다 | **체크리스트** — 「최솟값·최댓값·횟수」로 얻은 값은 `≥ N [관측 하한]` / `≤ N` 으로 **부등호를 붙여** 적는다. 등호로 바꾸는 것은 **소스 직독 · 후킹** 뿐이다. ⚠ 「표본이 전부 같다」는 하한이 안정됐다는 뜻이지 값이 맞았다는 뜻이 아니다 | Client · Design |
| 135 | **씬 그래프의 «노드 종류»로 «내용»을 판정한다** — 지형이 「메시 한 장」이라서 「타일맵이 아니다」로 적고 **확정표까지 그렇게 확정**했다. 실제로는 정점이 격자 간격으로 놓인 **타일맵**이었고 메시는 배칭 수단일 뿐이었다 | **절차** — 렌더 노드를 보면 **버퍼(정점·인덱스·UV)를 연다.** 정점 간격이 일정하면 격자이고, UV 가 몇 종류인지 세면 타일 종류다. 「어떻게 그리나」와 「무엇을 그리나」를 **따로 적는다** — 확정표는 후자로 정한다 | Fx · PD |
| 136 | **이관 범위를 「한 판」으로 잡았는데 «끝나는 조건»을 못 박지 않았다** — 분해표의 마지막 단위(한 판의 종료)가 **8회차 동안 비어 있었다.** 끝까지 살아서 가 보지 못했고, 「한 판」이 시간인지 목표인지 연쇄 과제인지를 아무도 묻지 않았다 | **게이트** — G0-b 에서 **「한 판이 끝나는 조건」** 행에 `[소스]`/`[실측]` 근거가 없으면 분해표를 닫지 않는다. 미측정이면 **「학습 부족」 표의 첫 줄**로 올리고, 소스가 안 내려오면 «끝까지 가는 봇»을 먼저 만든다 (분석로직 「시스템 축 점검표」 목표·퀘스트 축) | PD · Client |
| 137 | **남은 일이 있는 채로 「완료」를 말한다** — 「핵심 파리티는 닫혔고 셋이 남았다」를 완료처럼 보고했다. 다음 회차는 «완료»만 읽고 **그 셋을 잊는다** | **체크리스트** — 이관 전체의 «완료»는 PD 「«완료»는 마지막에 한 번」의 조건표가 **전부** 참일 때만 쓴다. 하나라도 비면 「완료」 대신 **남은 것의 목록**을 낸다 | PD |

| 138 | **UI «움직임»(버튼 펄스·호버·디머·등장 트윈)을 이관율 분모에서 통째로 뺀다** — 연출 문서에 프레임 애니메이션과 팝업만 적고, 창마다의 «숨쉬는 버튼»·«포인터 올라갔을 때»·«뒤를 덮는 검은 반투명»을 한 줄도 안 센다. 요소 전수 대조표는 «있나»만 묻고 «움직이나»는 안 묻는다. 정지 캡처엔 원리적으로 안 찍혀서 **100% 를 선언한 뒤 사람이 「또잉또잉이 없다」고 알려 줬다** | **게이트** — 학습 산출물 `05` 에 **「UI 연출」 표(창마다 디머 α · idle 펄스 식 · 호버 · 눌림 · 트윈)**를 두고 학습 완료 판정에 「창마다 한 줄 · 빈칸 0」을 건다. 이관율 대조표에 **「UI 연출」 갈래**를 반드시 둔다. 소스가 있으면 `pointerenter` · `Math.sin(` · `alpha:` · `scale.set(` 을 grep 한다 | Fx · UIUX · PD |
| 139 | **「아트 재제작」을 «의도된 차이»로 닫고 «퀄리티 동일» 판정을 안 한다** — 저작권으로 원본 에셋을 못 쓰면 재제작이 맞다. 그런데 코드로 찍은 사각형 인형을 «재제작 완료»로 두고, 확정표의 합격 기준(«퀄리티 동일»)을 **한 번도 원본과 나란히 놓고 보지 않았다.** 채점기는 «있나·크기·주소»만 재므로 그림의 질은 어떤 숫자에도 안 나온다 | **게이트** — 아트 재제작이 확정된 게임은 마감 전에 **「원본 참고 시트 vs 우리 시트」 대조 시트**(Assets 밖 · 스크래치패드)를 만들고 **사람이 한 번 본다.** 참고 시트는 «화면에 보이는 것»을 떠서 «보기용»으로만 쓴다 — Assets 에 넣지 않는다. 이관율 대조표에 **「아트 퀄리티 동일」 행**을 둔다 | Transfer_Artist · PD |

| 140 | **검사기가 끝에서 `EditorApplication.Exit` 를 «무조건» 부른다** — 배치모드용으로 짠 마무리를 «열린 에디터에게 시키는» 경로(다리)에서도 그대로 타면, 검사는 통과했는데 **사람이 쓰던 에디터가 꺼진다.** 저장 없이 꺼졌으니 다음에 열 때 **「_recovery backup scene?」** 대화상자가 떠서 사람이 눌러야 하고, 러너는 「에디터가 안 잡힌다」며 다음 검사를 배치로 돌려 **끄고 켜기가 반복**된다 | **코드** — 검사기의 마무리는 「다리가 시킨 실행인가」를 먼저 본다: 재생 검사는 `ExitPlaymode` 만, 편집 검사는 아무것도 안 부른다. ⚠ 다리는 그 표식을 **편집 모드 실행 동안에도** 켠다 — 재생 때만 켜면 편집 검사기가 같은 사고를 «두 번째로» 낸다. **러너** — ① 편집 검사·굽기는 다리로(닫지 않는다). 재생 검사는 배치가 필수다 — 뒤에 있는 에디터의 재생은 틱이 느려 검사가 멈춘다 ② 꺼야 하면 «저장 후 정상 종료»를 먼저 시키고 응답 없을 때만 강제 종료 ③ 열기 전에 복구 백업(`Temp/__Backupscenes`)을 치운다 = 대화상자에 «No». **원칙** — 이관 중 에디터를 끄는 것은 빌드처럼 배치가 «필수»일 때뿐이다 | Transfer_Programmer · PD |

| 141 | **돌고 있는 셸 러너를 «고친다»** — 긴 배치(닫기→검사→열기)를 배경으로 띄워 놓고 그 사이 러너 스크립트를 편집하면, 셸은 파일을 «읽어 가며» 실행하므로 편집으로 밀린 자리에서 **엉뚱한 줄을 명령으로 읽고 죽는다.** 실측 사례: 재생 검사는 끝났는데 «다시 열기» 단계가 사라져 에디터가 닫힌 채 남았고, 복구 백업도 그대로였다 | **체크리스트** — 러너·빌드 스크립트는 **그 스크립트로 띄운 작업이 끝난 뒤에** 고친다. 고쳐야 하면 사본으로 고쳐 두고 작업이 끝나면 바꿔 넣는다. 배경 작업 목록에 «러너가 돌고 있다»를 적어 둔다 | 전원 |

| 142 | **오버레이(디머·페이드·창)가 덮인 화면에서 «바탕 색»을 잡는다** — 시작 게이트가 화면 전체를 검정 α.5 로 덮고 있는데 그 캡처에서 타일 색을 집어 팔레트로 썼다. 우리 화면은 그 위에 디머를 «또» 얹어 두 배로 어두웠다. 같은 자리에서 «지형이 놓는 개체»(나무)도 장식으로 넘겨 시스템 하나를 통째로 놓쳤다 | **체크리스트** — 색·밝기를 재는 캡처는 **오버레이가 없는 상태**(디머·페이드·모달 없음)여야 한다. 창이 떠 있으면 그 값은 «결과색»이지 자산색이 아니다(공통절차 「색·모양·크기는 소스 자산을 꺼내서 잰다」). 첫 화면 캡처의 «모든 개체»는 원본 정의(소스·씬 그래프)와 1:1 로 센다 — 배경처럼 보이는 것이 상호작용 개체일 수 있다 | Fx · UIUX · PD |

| 143 | **시트 컷을 «한 장씩» `await` 로 읽는다** — 어드레서블 로드가 컷마다 한 번 왕복이라, 세트 수십 개 × 컷 수십 장이면 첫 화면이 «몇 초» 늦게 뜬다. 사람은 「재생을 눌렀는데 한참 뒤 캐릭터가 나온다」로 본다. 지형·시뮬을 의심하기 쉽지만 재 보면 로드다 | **도구** — 로더에 «묶음 읽기»(핸들을 전부 띄운 뒤 `WhenAll`)를 두고 시트 하나·타일셋·아이콘 묶음은 그것으로 읽는다. 세트들도 «먼저 띄우고 나중에 받는다». **체크리스트** — 초기화에 **단계별 시간 로그**를 남긴다(데이터 · 아트 · 지형 · 아이콘 · 합계) — 「늦다」는 지적에 «어디서»로 답할 수 있어야 한다 | Transfer_Programmer |

| 144 | **분석 입력을 «스크린샷»으로 삼는다** — 이미지는 장당 토큰이 가장 많이 들면서 값이 아니라 «인상»만 남긴다. 좌표를 눈대중으로 옮겨 적다 틀리고(창 배치 15px), 오버레이 아래 색을 자산색으로 잡고, 배경처럼 보이는 개체를 놓친다. 정작 원본 엔진은 그 프레임의 «정확한 값»을 들고 있었다 | **절차** — 원본은 «플레이하면서» 사건 프레임의 씬 그래프·시계열·버퍼를 **텍스트**(TSV·트리)로 뜬다(공통절차 「사건 프레임 덤프」). 우리 쪽도 같은 표로 떠서 diff 한다. 이미지는 마지막 눈 확인 한 장. 형식은 자료 모양대로 — 표 TSV · 계층 트리 · 들쭉날쭉 JSON, **XML 금지**(같은 정보에 2.5배) | 전원 · PD |

| 145 | **한 프레임에서 «자리»를 읽고 고정으로 옮긴다** — 화면 가장자리를 도는 표식(목표 화살표·경고 아이콘)이 그 캡처에서 왼쪽 아래에 있었다는 이유로 「고정 좌표 (58, 519)」로 다섯 회차 동안 적혔다. 정지 화면에서는 «고정»과 «지금 거기 있음»이 구별되지 않는다 | **체크리스트** — 좌표를 확정하기 전에 **다른 상태의 프레임 두 장 이상**에서 같은 노드를 본다(사건 프레임 덤프가 이걸 공짜로 준다). 두 장에서 값이 다르면 좌표가 아니라 **식**을 찾는다 — 원본 코드의 `update` 를 연다 | UIUX · Fx |
| 146 | **연출 값을 «한 벌»로 뭉뚱그린다** — 떠오르는 문구가 여섯 갈래인데 수명·속도·글자 크기·색·흔들림이 갈래마다 다른 것을 한 벌(0.35초·17.5px)로 옮겨, 화면에는 나오지만 **전부 원본과 다른** 상태가 됐다. 게다가 갈래 하나(피격 팝업)는 통째로 빠졌는데도 「팝업 있음」으로 세어 이관율에 안 걸렸다 | **데이터** — 같은 «종류»의 연출이 둘 이상이면 **표 한 장에 갈래별 행**으로 만든다(코드 `const` 도, 뷰 한 벌도 아니다). **게이트** — 이관율 분모는 «연출 종류»가 아니라 **원본 클래스/갈래 수**로 센다: 소스에서 그 연출을 만드는 지점을 `grep` 해 개수를 맞춘다 | Design · Fx |

| 147 | **자동 검사가 «사람 손»에 흔들린다** — 배치 재생 창 위에 진짜 마우스 커서가 있으면 그 자리의 버튼에 호버가 얹히고(배율이 커진 채로 찍힌다), 클릭까지 들어오면 시작 게이트가 저절로 눌려 «첫 프레임»이 통째로 달라진다. 같은 코드로 두 번 돌렸는데 결과가 달랐다 | **도구** — 재생 검사는 시작할 때 **이벤트 시스템을 끈다**(조작은 전부 검사기가 `onClick` 을 직접 불러서 한다) + 프레임을 뜨기 직전에 호버 상태를 전부 내린다. **체크리스트**: 재생 검사 결과가 «두 번 돌렸을 때 다르면» 게임이 아니라 «입력 경로»를 먼저 본다 | 전원 |

| 148 | **버튼을 «손으로» 조립하다 `raycastTarget` 을 안 켠다** — 스프라이트 헬퍼가 장식용이라 `raycastTarget = false` 로 내는데, 그 위에 `Button` 만 얹으면 **컴포넌트는 다 붙어 있고 포인터만 통과**한다. 뒤에 깔린 디머가 대신 맞으므로 **화면은 완전히 정상**이고, 프리팹 검사·데이터 검사·플로우 검사가 전부 통과한다. 실측 사례: 확정 버튼 하나가 안 눌려 **게임이 정지한 채 멈췄고**, 같은 병이 **버튼 여섯 개**에 있어 모달 네 개가 통째로 먹통이었다 | **코드** — 버튼을 «손으로» 조립하지 않는다. **조립 함수 하나**(`Button` 추가 + `targetGraphic` 연결 + **`raycastTarget = true`**)만 지나게 하고, 프리팹 빌더의 모든 버튼이 그것을 부른다. **검사** — UI 프리팹 전수로 «버튼마다 `targetGraphic` 이 있나 · `raycastTarget` 이 켜졌나 · 클릭 받을 크기인가 · 디머가 앞에 있지 않나»를 센다(속성 검사라 편집 모드로도 정확하다). ⚠ 그리고 **재생에서 실제 포인터를 흘려** 한 번 더 닫는다 — 속성이 맞아도 무엇이 «위»에 있으면 안 맞는다 | Transfer_Artist · Transfer_Programmer |

| 149 | **결정적인 것을 「시드가 달라서 그렇다」로 «의도된 차이»에 등재한다** — 절차 생성 지형을 옮기며 노이즈 식·주파수·시드 «문자열»까지 소스대로 옮겨 놓고, **시드 난수원(PRNG)만** 우리 것을 썼다. 그리고 그 사실을 「생성 방식은 같고 무늬만 다르다 — 의도된 차이」로 적었다. 실제로는 **원본이 3회 독립 실행에서 소수점까지 같은 자리**였고, 사람 눈에는 **시작 화면부터 다른 게임**으로 보였다. 「의도된 차이」로 적힌 항목은 다음 회차가 **다시 재지 않는다** | **절차** — 「무작위라서 다르다」를 등재하기 전에 **원본을 n≥3회 다시 열어 «같은 자리에 같은 것이 나오는지»**부터 본다. 같으면 그것은 차이가 아니라 **안 옮긴 것**이다. **PRNG 는 «식»이라 옮길 수 있다** — 번들 소스에서 난수 생성기를 찾아(`#127`) 그대로 이식하고, **원본 코드를 그대로 돌려 뽑은 골든 벡터**(난수열 · 노이즈 표본 · 배치 좌표)로 닫는다. ⚠ 「아트가 재제작이라 픽셀 동일이 기준이 아니다」는 **배치·좌표까지 면제하지 않는다** | Client · Fx · PD |

| 150 | **대체 서체의 «자폭»이 원본과 다른 것을 안 잰다** — 폰트를 못 구해 유사 서체로 대체하면서 «글자 크기»만 원본과 맞췄다. 그런데 원본이 **고정폭 픽셀 서체**(모든 글자가 1em)였고 대체 서체는 **비례폭**이라, 같은 자리에 같은 크기로 놓아도 **문구 폭이 원본의 45~68%** 로 나왔다 — 숫자열(타이머·게이지)이 특히 심했다. 좌표·크기 검사는 «글자 상자»를 안 재므로 한 항목도 안 걸린다 | **체크리스트** — 서체를 대체하면 **글리프 폭 ÷ (글자수 × fontSize)** 를 문구 몇 개로 재서 원본과 비교한다. 1.0 근처로 일정하면 원본이 **고정폭**이다. **코드** — 그 사실은 **폰트 에셋이 든다**(글리프 advance 를 1em 으로 굽는다) — 프리팹 필드나 리치 텍스트 태그로 두면 «호출부마다» 챙겨야 해서 언젠가 빠진다. ⚠ 자간(`characterSpacing`)으로 늘리면 글자 사이만 벌어져 숫자열이 성기게 보인다 | UIUX · Transfer_Artist |

| 151 | **타일 «종류»를 줄여도 화면은 정상으로 보인다** — 절차 지형의 타일 선택 규칙을 옮기며 자동 타일링 갈래를 일부만 만들었다(길 가장자리 8종 · 안쪽 모서리 4종 누락). 없는 종류는 **가장 가까운 다른 타일로 대체**되므로 화면은 «지형처럼» 보이고, 「칠한 칸 수 = 창 크기」·「같은 칸 = 같은 타일」 같은 검사는 **전부 통과**한다. 그런데 화면의 대부분이 지형이라 **사람 눈에는 가장 크게 다른 부분**이다 | **절차** — 타일 규칙은 «분기마다 반환하는 값»을 **전수로 세어** 표로 만든다(소스의 `tileId` 상수를 전부 `grep`). 우리 열거의 칸 수가 그 수와 다르면 **그 차이가 곧 누락**이다. **검사** — 「타일 종류 수 = 원본 분기 수」를 항목으로 둔다. ⚠ 이웃을 보는 패스(자동 타일링·목 제거)는 **순서가 곧 규칙**이다 — 조건 순서를 바꾸면 같은 이웃 배치에서 다른 타일이 나온다 | Fx · Transfer_Artist |

| 152 | **「원본을 보고 그렸다」를 근거로 삼는다** — 아트를 재제작하는 게임에서 원본을 «보고» 도트를 손으로 찍고 그것을 재제작 완료로 뒀다. 실제로 재 보니 **실루엣이 절반만 겹쳤고**(IoU 평균 50.6%) 색 수는 원본의 3분의 1이었다(19색 → 7색). 크기·주소·컷 수만 세는 채점기는 **전부 통과**시켰고, 사람이 화면을 보고 「생김새가 너무 다르다」고 말해서야 드러났다. ⚠ 그리고 이때 「강화학습으로 그림 실력을 올리자」는 방향이 나오는데, **모델은 세션 중에 학습하지 않는다** — 다음 회차의 담당자는 오늘 찍은 도트를 기억하지 못한다 | **절차 + 채점기** — ① 원본 아틀라스를 캔버스로 올려 컷마다 **실루엣 마스크 · 팔레트(색+비중) · 채움률 · 고유색 수**를 «잰다» (에셋을 넣는 것과 규격을 재는 것은 다른 일이다) ② 그 규격을 **목표로 굽는다** — 마스크대로 채우고 팔레트 비중대로 면적을 나눈다 ③ 채점기가 **실루엣 IoU · 팔레트 거리 · 색 수 비**를 재고 **합격선을 확정표에 숫자로** 못 박는다. ⚠ **선을 긋는다** — 실루엣·팔레트는 실측 그대로 목표로 쓰되 «어느 픽셀에 무슨 색을 놓을지»는 우리 규칙이다(원본 픽셀 배치를 옮겨 적으면 재제작이 아니라 복제다). ⚠ 「학습」으로 해결하려 하지 말고 **점수를 만든다** — 점수가 있으면 루프는 도구가 돈다 | Transfer_Artist · PD |

| 153 | **「시트를 구웠다」와 「화면이 그것을 «쓴다»」를 같은 것으로 센다** — 개체의 상태별 시트를 규격대로 다 굽고 이관율에 «있음»으로 세었는데, **뷰가 한 장만 그리고 있었다.** 실측 사례: 서기·달리기 시트를 구워 놓고도 구조된 동료가 **누운 그림으로 따라다녔다**. 아트 채점기(형태·색)도 데이터 검사(행 수·주소)도 안 걸린다 — 둘 다 «그림이 맞나»만 보고 **«쓰이나»는 안 본다**. 같은 뿌리로 여러 «명»이 서 있어야 할 자리에 한 명만 서기도 한다 | **검사** — 재생에서 **상태를 실제로 전이시켜** ① 상태값이 바뀌나 ② **스프라이트가 «다른 시트»로 갈리나** ③ 같은 상태에서 **컷이 도나** 를 잰다. 여럿이 서는 개체는 **개수 · 서로 다른 시트 · 서로 다른 자리**를 함께 센다. **체크리스트** — 시트를 새로 구우면 「이 시트를 «읽는 곳»이 어디인가」를 한 줄 적는다(`#131` 과 같은 질문이다). 읽는 곳이 없으면 **아직 안 옮긴 것**이다 | Transfer_Artist · Transfer_Programmer |

| 154 | **«내가 만든 지표»가 100% 인 것을 «완료»로 보고한다** — 아트 재제작을 재려고 실루엣 IoU·팔레트 거리를 만들고 **둘 다 만점**을 받자 완료라고 했다. 그런데 실루엣은 「윤곽 «안»인가 밖인가」만 보고, 팔레트는 「그 색이 «쓰였나»」만 본다 — **그 안을 무엇으로 칠했는지는 어느 쪽도 안 본다.** 사람이 원본을 옆에 놓고 「이게 완벽이냐」고 물어서야 재 봤고, **픽셀 일치는 13~34%** 였다. 지표가 통과시킨 것이지 그림이 맞은 것이 아니었다 | **채점** — 「같은가」를 재는 항목은 **결과물의 «최소 단위»로** 잰다. 그림이면 **픽셀 일치율**이고, 실루엣·팔레트는 그 «부분 지표»일 뿐이다. ⚠ **지표를 새로 만들면 「이 지표가 100% 인데도 틀릴 수 있는 경우」를 한 줄 적는다** — 못 적으면 그 지표를 아직 이해 못 한 것이다. **체크리스트** — 사람에게 「완료」라고 말하기 전에 **원본과 나란히 한 번 본다**(`#139`). 눈으로 다른 것이 숫자로 100% 면 **숫자가 틀린 것**이다 | 전원 · PD |

| 155 | **작은 도트에서 「형태는 맞추되 픽셀은 우리 것」이 성립한다고 본다** — 저작권을 지키려 「실루엣·팔레트는 실측 그대로, 픽셀 배치는 우리 규칙」이라는 선을 그었다. 그런데 **16×16 에서는 픽셀 배치가 곧 형태다** — 눈이 2픽셀, 입이 1픽셀이라 «칠하는 규칙»이 낄 자리가 없다. 그 선을 지키는 한 사람 형태가 안 나오고, 회차를 아무리 돌려도 「덩어리」에서 못 벗어난다 | **판단** — 스프라이트가 작을수록(≤32px) 「형태만 맞추기」는 **성립하지 않는다**. 어디까지 맞출지는 **기술 문제가 아니라 정책 문제**이므로 **사람에게 묻고 확정표에 적는다**. ⚠ 담당자가 임의로 선을 긋고 그 선 안에서 「완료」를 선언하지 않는다 — 사람이 보는 기준과 어긋난 채 회차만 돈다 | Transfer_Artist · PD |

| 156 | **에디터 재생 검사마다 «껐다 켠다»** — 「뒤에 있는 에디터의 재생은 틱이 느려 검사가 몇 분씩 멈춘다」를 실측으로 확인하고, 재생 검사는 **배치가 필수**라고 절차에 못 박았다. 그 뒤로 검사 한 번마다 «저장 종료 → 배치 → 다시 열기»로 **5~8분**을 썼고, 사람은 화면이 반복해서 사라지는 것을 본다. ⚠ 그런데 그 느림의 원인은 다리가 아니라 **`runInBackground` 가 꺼져 있던 것**이었다 — 엔진은 포커스를 잃으면 재생을 throttle 하고, 셸이 앞에 있으면 «항상» 그렇다 | **코드** — 재생 검사기가 시작할 때 **런타임에서** 백그라운드 실행을 켠다(프로젝트 설정을 바꾸지 않으므로 빌드에 영향이 없다). 그러면 **열린 에디터에게 시켜도 배치와 같은 속도**로 돈다 — 실측 5~8분 → **53초**. **체크리스트** — 「이 도구는 원래 느리다」를 절차로 굳히기 전에 **왜 느린지**를 한 번 캔다. 성능 결론은 «설정 하나»로 뒤집히는 일이 흔하다 | Transfer_Programmer · PD |

| 157 | **엔진이 다른 두 «텍스트 경계»를 같은 값으로 비교한다** — 문구 폭을 대조하는데 원본은 **stroke 를 포함한 렌더 결과**를 주고, 우리 엔진은 **stroke 없는 글리프 사각형**을 준다. 기준을 맞춘다며 글리프 사각형으로 바꾸자 **한 글자 숫자가 원본의 37%** 로 찍혔다 — 획만 셌기 때문이다. 어느 쪽도 «틀린» 값이 아닌데 비율만 보고 게임을 고치러 간다 | **체크리스트** — 텍스트는 **«자리»(중심·모서리)를 엄격히, «폭»은 느슨히**(±10% 수준) 본다. 자리는 앵커·여백이라 엔진이 달라도 같은 값이 나오지만, 폭은 «무엇을 경계로 보느냐»가 엔진마다 다르다. ⚠ 폭이 어긋나면 **서체 자폭(advance)** 을 먼저 재라 — 폰트 에셋의 실제 advance 를 읽어 「넣은 값」과 「들어간 값」을 가른다. 그래도 남는 차이는 **서체 차이로 등재**하고 그림을 고치지 않는다 | UIUX · Transfer_Artist |

| 158 | **원장의 «옛 표»를 안 지우고 새 절만 쓴다** — 회차마다 「지금 상태 한 장」을 새로 쓰면서 리스트업·재측정 대기·QA 미흡 표는 그대로 두었다. 그러자 **같은 원장에 「이관율 100%」와 「달성률 60~85%」가 공존**하고, 이미 만들어 둔 화면이 **「미착수」로 남아** 있었다. 사람이 「완료된 건가?」라고 물었을 때 **바로 답할 수 없었다** — 검사는 전부 통과인데 원장이 아니라고 말하고 있었다 | **체크리스트** — 회차를 닫을 때 **새 절만 쓰지 않는다.** ① 리스트업 ② 재측정 대기 ③ QA 미흡 — **세 표를 같이 손본다.** 닫힌 줄은 지우지 말고 «취소선 + 어느 회차에 무엇으로 닫혔는지»를 남긴다(근거가 사라지면 다음 회차가 다시 잰다). ⚠ **완료 판정은 «검사 결과»가 아니라 «원장»이 한다** — 검사는 그 시점의 코드를 재고, 원장은 «무엇이 남았는가»를 든다. 원장이 낡으면 통과한 검사가 완료를 증명하지 못한다 | PD |

| **159** | **★★ 「만들었다」를 「옮겼다」로 센다 — 이 세트의 사고 중 가장 많은 뿌리** | 아래가 전부 «같은 사고»다. 개별 항목(`#131`·`#148`·`#153`·`#154`)으로 흩어져 있었지만 뿌리는 하나다 — **산출물을 세고 «소비 지점»을 안 센다.** 만든 쪽에서 판정하니 전부 통과하고, 화면에서만 틀린다 | **규격 + 체크리스트 + 게이트** — 아래 세 겹 |

> **한 회차에 동시에 있었던 넷** — 그리고 **모든 검사가 통과했다.**
>
> | 만든 것 | 빠진 소비 지점 | 화면에서는 | 개별 항목 |
> |---|---|---|---|
> | 버튼 컴포넌트를 붙였다 | 포인터를 받게 안 켰다 | 눌러도 아무 일이 없다 (모달 4종 먹통) | `#148` |
> | 상태별 시트를 구웠다 | 뷰가 한 장만 읽는다 | 상태가 바뀌어도 그림이 그대로 | `#153` |
> | 문구를 표에 넣었다 | 읽는 곳이 없다 | 그 UI 가 통째로 없다 | `#131` |
> | 형태·색 지표가 만점 | 그 지표가 «그림»을 안 잰다 | 다른 그림 (픽셀 일치 13~34%) | `#154` |
>
> ⚠ **네 번 다 「사람이 화면을 보고」 알려 줬다.** 검사·이관율·달성률 어느 것도 안 걸렸다.

**막는 세 겹:**

| 겹 | 무엇 |
|---|---|
| **① 규격** | 이관율 대조표에 **「화면에 나옴을 무엇이 증명하나」** 열을 둔다. 비면 **분자에 안 넣는다** (분석로직 「우리에게 있다로 세지 않는다」) |
| **② 체크리스트** | 담당자는 **자기가 만든 것마다 «읽는/쓰는 곳»을 한 줄** 적는다 — 표의 **모든 컬럼**(주소·키만이 아니다) · 구운 **모든 시트** · 붙인 **모든 컴포넌트** |
| **③ 게이트** | 마감 전에 **상태를 실제로 전이시켜** 「그림이 갈리나 · 눌리나 · 값이 바뀌나」를 재생에서 잰다. 정적 검사는 이 종류를 원리적으로 못 잡는다 |

⚠ **「만든 것」과 「쓰이는 것」의 개수를 따로 센다.** 둘이 같아야 이관이다 —
표 행 수 · 구운 시트 수 · 프리팹 수는 **분모**이고, 그것을 읽는 코드 지점 수가 **분자**다.

### 등재 대기 — 아직 대책이 도구/게이트가 아닌 것

| 관찰 | 왜 아직 등재 못 하나 |
|---|---|
| 문서 세트가 **「이미 있는 프레임워크」를 전제**해 신규 프로젝트에서 구축 회차가 누락된다 | 대책이 「최소 세트 정의」인데 아직 문서에 안 들어갔다. 들어가면 등재한다 |
