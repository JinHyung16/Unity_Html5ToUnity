# CLAUDE.md

## 이 저장소

HTML5 게임을 Unity 로 이관하는 저장소다. **게임을 여러 개 이관한다.**

| 무엇 | 어디 |
|---|---|
| **이관 오케스트레이션** | **`HtmlToUnity/`** — 스킬 본문. **이 저장소의 본체이자 커밋 대상** |
| Unity 프로젝트 | 저장소 루트 (`Assets/` · `Packages/` · `ProjectSettings/`) · Unity 6000.3.13f1 · URP 17.3.0 |
| 원본 HTML5 게임 풀 | `C:\jinhyeong_project\Html Games 모음\` |
| 스킬 **작업본** | `.claude/skills/htmltounity/` — 내용은 위와 **같게 유지**한다 (`HtmlToUnity/sync.sh`) |
| 게임별 원장 | `.claude/HtmlToUnity_작업내역_<게임명>.md` — **개인 작업 기록이라 커밋하지 않는다** |

### ★★ 오케스트레이션은 **두 곳에 똑같이** 둔다

`.claude/` 는 **`.gitignore` 에 들어 있다** — MCP 설정에 OAuth 비밀값이 들어갈 수 있어서다.
그런데 Claude Code 는 **스킬을 `.claude/skills/` 에서만 찾는다.**
그래서 **같은 문서를 두 곳에 둔다.**

| 어디 | 왜 필요한가 | 커밋 |
|---|---|---|
| `.claude/skills/htmltounity/` | Claude Code 가 스킬을 여기서 찾는다 | ❌ (`.gitignore`) |
| `HtmlToUnity/skills/htmltounity/` | 커밋되고 남의 손에 건네진다 | ✅ **여기만 커밋한다** |

**문서를 고치면 두 곳을 «같이» 최신화한다.** 한쪽만 고치면 다음 게임이 낡은 쪽을 읽는다.

```bash
bash HtmlToUnity/sync.sh                 # HtmlToUnity → .claude (기본)
bash HtmlToUnity/sync.sh --from-claude   # .claude → HtmlToUnity
bash HtmlToUnity/sync.sh --check         # 둘이 같은지 확인 (작업 끝에 반드시)
```

⚠ **작업을 끝내기 전에 `--check` 를 돌린다.** 「고쳤다」가 아니라 **「둘이 같다」**가 완료 조건이다.

⚠ **원장은 동기화 대상이 아니다.** 원장(`.claude/HtmlToUnity_작업내역_<게임명>.md`)은
**그 사람이 그 게임을 돌린 기록**이라 사람마다·회차마다 다르다.
오케스트레이션에 끼워 넣으면 받는 사람에게는 남의 작업 기록일 뿐이다 — **커밋하지 않는다.**

## ★★ 커밋은 **시킬 때만** 한다

**깃 커밋·브랜치·푸시를 스스로 판단해서 하지 않는다.**
사람이 **「커밋해라」라고 말한 그 범위만** 커밋한다.

| ✗ | ✓ |
|---|---|
| 「정리된 김에 커밋해 두자」 | 시킬 때까지 **작업 트리에 그대로 둔다** |
| 시킨 것 + 내 판단으로 더 담기 | **시킨 것만** 담는다 |
| 브랜치를 임의로 파고 옮겨 두기 | 브랜치도 **시킬 때만** |

⚠ **브랜치를 옮기면 작업 트리의 파일이 사라질 수 있다.** 커밋되지 않은 것이 있으면
사람이 보던 상태가 통째로 바뀐다 — **시키지 않은 브랜치 조작을 하지 않는 이유가 이것이다.**

## 이관 작업

**HTML5 → Unity 이관·파리티 검증 요청이면 `htmltounity` 스킬을 먼저 연다.**

절차는 여기 적지 않는다. 두 곳에 있으면 한쪽이 반드시 낡는다 —
확정 · 배분 · 게이트 · QA 는 전부 `HtmlToUnity/skills/htmltounity/references/PD.md` 가 정본이다.

---

## 폴더 규칙

```
Assets/
├─ Scripts/                 ← 공용. 어떤 게임이 와도 클라가 쓴다
│   ├─ Core/                   GameRoot · Manager · Management
│   ├─ UI/                     Window · Panel · Component · 풀링
│   ├─ Data/                   JSON 로더 · Container
│   ├─ Extensions/             Extensions.cs 등 확장 메서드
│   └─ Editor/                 공용 에디터 툴 · 프리팹 빌더 베이스
└─ <게임명>/                 ← 게임 하나당 한 폴더
    ├─ Scripts/
    ├─ Prefabs/
    ├─ Art/
    ├─ Data/
    └─ Editor/                 그 게임의 프리팹 빌더
```

- **게임 폴더 이름에서 번호를 뗀다.** `01-Candy-Crush-Game` → **`Assets/Candy-Crush-Game/`**
- **공용은 게임 폴더 밖이다.** 두 번째 게임이 그대로 쓸 수 없으면 공용이 아니다.
- 게임 하나에서만 쓰는 것을 `Assets/Scripts/` 에 올리지 않는다.

## 코드 규칙

**확장 메서드로 판정을 읽히게 쓴다.** `Assets/Scripts/Extensions/Extensions.cs`.

```csharp
// 이렇게 쓰지 않는다
if (list == null || list.Count == 0) { … }

// 이렇게 쓴다
if (list.IsNullOrEmpty()) { … }
```

새 확장을 만들 때는 **먼저 `Extensions.cs` 에 같은 것이 있는지 본다.** 중복 확장이 두 개 생기면
호출부마다 다른 쪽을 쓰게 되고 동작이 갈린다.

### 네이밍 · 네임스페이스

**`~/.claude/skills/coding-conventions.md` 가 정본이다.** 여기 사본을 두지 않는다.
그 중 이 저장소에서 자주 부딪히는 둘만 못 박는다.

| 규칙 | 값 |
|---|---|
| **enum** | **`E` 접두사** — `EWindowType` · `EGameScreenType` |
| 네임스페이스 | 루트 `JinHyung` · 기능별 하위 — `JinHyung.Core` · `JinHyung.UI` · `JinHyung.Data` · `JinHyung.Extensions` |

> 스킬 문서(`DataFramework.md`·`Design.md`)에 한때 「`E` 접두 금지」가 있었으나
> **프로젝트 컨벤션이 우선**이라 규약 문서 쪽을 고쳤다. 다시 어긋나면 규약 문서를 고친다.

### 로그 — `Log.cs`

**`Debug.Log` 를 직접 쓰지 않는다.** `JinHyung.Core.Log` 를 쓴다.

```csharp
Log.Success("보드 8x8 생성 완료");
Log.Warning("사탕 스프라이트가 비었다");
Log.Error("데이터 행 수가 원본과 다르다");
```

세 메서드 전부 `[Conditional("UNITY_EDITOR")]` 다 — **빌드에서 호출부와 인자가 통째로 제거**된다.
지우는 것을 깜빡한 로그가 남아 있어도 빌드에 안 들어간다.

> ⚠ **출시 빌드에 남아야 하는 오류 로그에는 쓰지 않는다.** 그건 별도 경로가 필요하다.

### 컴파일 확인

**유니티 에디터 없이 확인할 수 있다.** 유니티가 만든 `.csproj` 는 방금 추가한 파일을 모르므로
그걸로 빌드하면 **새 파일을 빼고 통과**한다. 스크래치패드에 임시 csproj 를 세워
유니티 DLL 을 직접 참조한다 (절차는 `공통절차.md` 「에디터를 못 돌려도 컴파일 판정은 된다」).

## 렌더 규칙

**색공간은 Linear 를 유지한다.** 프로젝트 표준이다.

> ⚠ **원본 CSS 알파를 역산하지 않는다.** 원본 캔버스는 감마 합성이라 Linear 에서 반투명이 어긋나는데,
> **어긋나는 만큼은 리소스 자체 또는 프리팹 세팅에서 흡수한다.**
>
> - **코드·데이터의 원본 값은 손대지 않는다** — 원본 알파는 원본 값 그대로 적고 그대로 둔다
> - 보정은 **눈에 보이는 자산(텍스처 · 프리팹 인스펙터)에만** 남긴다
> - 그래서 나중에도 「어느 값이 원본이고 어느 것이 보정인지」가 갈린다

## 로드 규칙

| 무엇 | 어디서 | 왜 |
|---|---|---|
| **UI 프리팹** (Window · Panel · Component) | **Resources** | 수가 적고 대개 항상 필요하다. 경로 = 식별자가 단순하다 |
| **그 밖의 아트** (스프라이트 · 배경 · 이펙트 · 사운드) | **Addressables** | 로드/해제 단위가 있어야 빌드가 안 부푼다 |
| **데이터 JSON** | Addressables 라벨 | `DataManager` 가 라벨 하나로 전부 읽는다 |

**두 경로가 섞이는 것은 의도된 것이다.** 다만 코드에서 어느 쪽인지 보여야 한다 —
어드레서블 키는 이름에 `~Address` / `~Key` 를 붙여 Resources 경로와 구분한다.

## 검증 산출물 규칙

**검증용 파일·에셋을 유니티 프로젝트 안에 만들지 않는다.**
`_Verify/` · `GoldenVector.asset` 같은 생성물이 섞이면 **개발자가 아닌 사람은 커밋 여부를 판단할 수 없다.**
`.gitignore` 로 감추는 것도 답이 아니다 — 정답지는 감출 것이 아니라 **읽혀야 할 기록**이다.

| 무엇 | 어디 |
|---|---|
| 골든 벡터 (입력 → 기대 출력) | `<원본폴더>/HtmlToUnityLogic/07_기대값.md` — **md 표** |
| 원본 쪽 실측 | 브라우저 콘솔 |
| 유니티 쪽 실측 | **임시 `Debug.Log`** → 콘솔을 표에 옮기고 **지운다** |
| 대조 결과 | 원장에 `N/N` 숫자 |

**벡터가 수십 개를 넘거나 같은 것을 3회차 이상 다시 재고 있으면** 검사 코드를 만든다.
단 **`Assets/` 안에 두지 않는다** — 아래 규칙을 따른다.

### 에디터 도구는 두 갈래다

| 갈래 | 무엇 | 어디에 |
|---|---|---|
| **구성하는 도구** | 프리팹 빌더 · **UI 스프라이트 굽기** · 임포트 설정 · 어드레서블 등록 · 플랫폼 설정 | **`Editor/`** — 공용은 `Assets/Scripts/Editor/`, **그 게임 전용은 `Assets/<게임명>/Editor/`**. **남긴다** — 없으면 「사람이 인스펙터로·그림 편집기로」로 돌아간다 |
| **재는 도구** | 골든 채점기 · 플로우 검사 · 배선 검사 · 재생 검사 · 렌더 캡처 | **`Tools/Verify/`** (Assets 밖) — 실행할 때만 `Tools/unity-batch.sh` 가 잠깐 들여놓고 지운다. **이관이 끝나면 통째로 지운다** (아래) |

#### ★★ 재는 도구는 **이관이 끝나면 지운다**

**정답지는 `07_기대값.md` 의 표다. 채점기는 그 표를 읽어 주는 일회용이다.**
정답지가 남아 있으면 채점기는 **언제든 다시 만들 수 있다** — 그래서 남길 이유가 없다.

| 남는 것 | 사라지는 것 |
|---|---|
| `07_기대값.md` (골든 벡터 표) · 원장의 `N/N` 숫자 | `Tools/Verify/*.cs` |

- 마지막 회차에 **전 검사를 한 번 통과시킨 뒤** 지운다 — 지우고 나서 고치면 다시 못 잰다
- **「미측정」·「재측정 대기」 칸이 비어 있는지 같이 본다.** 검사가 다 통과해도
  «아직 안 잰 것»이 남아 있으면 이른 것이다 — 다시 만들어 재야 한다 (실제로 그랬다)
- 지웠다는 사실과 **마지막 통과 숫자**를 원장에 적는다
- 다시 재야 하면 **`07_기대값.md` 를 보고 채점기를 새로 만든다**

#### ★ 이관이 끝난 뒤 `Tools/` 에 남아도 되는 것

**`Tools/` 는 「저장소 전체가 쓰는 것」자리다.** 게임 전용이 여기 남으면 다음 게임 담당자가
**지워도 되는지 판단할 수 없다.**

| 남는다 | 옮긴다 · 지운다 |
|---|---|
| `Tools/unity-batch.sh` — **어느 게임에도 안 묶인 러너.** 문서 여러 곳이 이걸 부른다 | **그 게임 전용 굽는 도구** → `Assets/<게임명>/Editor/` 로 (C# 에디터 툴로) |
| | **재는 동안만 필요한 자리**(원본을 띄우는 폴더 등) → **지운다** |
| | `Tools/Verify/` — **지운다** |

> **[사고]** 이 게임 전용 스프라이트 생성기를 `Tools/Art/` 에 파이썬으로 두고 끝냈다.
> 「구성하는 도구라 남긴다」는 맞았지만 **자리가 틀렸다** — 게임 전용인데 저장소 공용 자리에 있었다.
> C# 에디터 툴로 옮겨 게임 폴더에 넣고, 배치로 굽는다.
> ⚠ **다시 구우면 픽셀이 달라질 수 있다** (라이브러리마다 안티에일리어싱·블러가 다르다).
> 옮긴 뒤에는 **렌더 판정을 다시 재서** 통과를 확인한다.

- **에디터 메뉴(`[MenuItem]`)를 만들지 않는다.** 사람이 메뉴를 안 누르는 것이 규칙이다 — 도구는 배치 실행 전용이다.
- **게임 코드에 검증용 진입점(`DebugSetXxx`)을 두지 않는다.** 이미 있는 주입 지점으로 세운다 —
  무작위는 **주입식 RNG**, 데이터는 **텍스트 주입 경로**, 시간은 **틱 함수 인자**.
  새 문을 뚫으면 이관 결과물에 치트가 남고, 검증이 **실제 경로를 안 지난다.**

## 확정표는 프로젝트 설정까지 가야 한다

**확정표에 적어 둔 답이 프로젝트 설정에 안 들어가면 그 확정은 종이에만 있는 것이다.**

| 확정 | 반영 자리 |
|---|---|
| 타깃 플랫폼 · 구체 플랫폼 | **빌드 타깃 전환** · 패키지 이름 · 최소 OS · 스크립팅 백엔드 · ABI |
| 화면 방향 | 기본 방향 + **자동 회전 4항목 전부** |
| 화면 비율 | Canvas Reference + **플레이어 기본 해상도** |
| 색공간 | 색공간 + **그래픽 API 목록** (Linear 는 구형 API 로 못 쓴다) |
| 리소스 해상도 정책 | **텍스처 압축 포맷** — 「4의 배수」의 근거가 여기다 |

반영은 `PlatformSetup` 이 **코드로** 한다. 반영 여부는 **배선 검증이 기계로 확인**한다 —
사람 기억에 맡기면 다음 회차에 누가 되돌려 놔도 모른다.

⚠ **원본에 없는 것이 엔진 기본값으로 켜져 있는 자리**를 같이 본다 (스플래시 화면 등).
「우리가 만든 것」만 보면 안 잡히고 플로우 diff 에도 안 걸린다 — 엔진이 그리는 것이라서.

## 원본 이상(버그) 규칙

**원본의 이상해 보이는 거동은 「그대로 재현」이 기본이다.**

1. 코드를 읽은 추측이 아니라 **원본을 돌려서 실측**해 확증한다
2. 학습 문서에 **[실측] 표시 + 재현 방법**을 적는다
3. 원장의 **「원본 이상 목록」**에 올린다
4. 고치려면 **합격 기준에 「의도된 차이」로 명시 등재**한다 — 등재 없이 고친 것은 결함이다

## 프레임워크 구축 규칙

**이 저장소는 프레임워크를 「최소 세트」로 쌓아 올린다.**

| 규칙 | 내용 |
|---|---|
| **그 게임이 실제로 부르는 것만 만든다** | 안 쓰는 코드는 검증도 안 되고 다음 게임에서 걸림돌이 된다 |
| 한 게임만 쓰면 **게임 폴더** | `Assets/<게임명>/Scripts/` |
| **두 번째 게임이 같은 것을 요구하면** 공용으로 승격 | `Assets/Scripts/` 로 올린다 |
| 「나중에 쓸 것 같아서」 공용에 올리지 않는다 | 두 번째 게임이 오기 전까지 아무도 검증하지 않는 코드다 |

## 프리팹 규칙

**프리팹은 빌더 스크립트로 굽는다.** 손으로 고친 것은 다음 빌드 실행에 날아간다.

- 프리팹을 고쳐야 하면 **빌더 스크립트를 고치고 다시 굽는다**
- 빌더는 게임 폴더의 `Editor/` 에 둔다
- 이 규칙을 바꾸려면(= 에디터 직접 편집으로 전환) 그 게임의 원장 확정표 10-b 를 먼저 고친다

## 도구 제약 — 지금 이 환경

| 항목 | 상태 | 그래서 어떻게 |
|---|---|---|
| **Unity 에디터 제어 (대화형)** | 없다 (MCP 미연결) | 아래 배치모드로 대체한다 |
| **Unity 배치모드** | **된다** | `Tools/unity-batch.sh <Namespace.Class.Method>` — 에디터 코드를 헤드리스로 실행한다. **사람에게 메뉴를 누르라고 하지 않는다** |
| **Unity 재생(Play) 검사** | **된다** | `PLAYMODE=1 Tools/unity-batch.sh …` — 배치에서 재생에 들어가 **실제 포인터 이벤트**를 흘린다. ⚠ **입력·레이캐스트는 여기서만 잰다** — 편집 모드에서는 캔버스가 안 그려져 레이캐스트가 **원리적으로 빈다** |
| 브라우저 스크린샷 | **된다** — 크롬 헤드리스 | 사람 없이 찍는다. **결정적**으로 만들려면 시드 고정 + 주기 진행 차단 + 레이아웃 크기 못 박기 (`공통절차.md`) |
| 원본을 **살려서** 띄우기 | **된다** | ⚠ `file://` 은 «정적 스냅샷»이 되어 스크립트가 안 돈다 → **로컬 HTTP 서버**로 연다 |
| 이미지 비교 | Pillow 12.3.0 | 픽셀 일치율 측정 가능 |

## ★ 사람에게 유니티 조작을 요구하지 않는다

**이관을 의뢰한 사람은 코딩도 유니티도 모를 수 있다.**
「메뉴를 눌러 주세요」·「인스펙터에서 값을 넣어 주세요」는 **절차가 아니라 실패다.**

| ✗ | ✓ |
|---|---|
| 「`Tools > …` 를 눌러 주세요」 | `Tools/unity-batch.sh` 로 **직접 실행** |
| 「인스펙터에서 라벨을 지정해 주세요」 | 에디터 API 로 **코드가 지정** (`AddressableSetup`) |
| 「프리팹을 만들어 주세요」 | **빌더 스크립트가 굽는다** |
| 「패키지를 설치해 주세요」 | `Packages/manifest.json` 을 **직접 고친다** |

**사람에게 남기는 것은 둘뿐이다** — ① 원본 게임을 건네주는 것 ② 마지막에 열어서 **보는 것**.

⚠ **에디터가 열려 있으면 배치모드가 실패한다.** 열려 있으면 **닫고 · 돌리고 · 다시 연다.**
닫기 전에 **저장 안 된 작업이 날아갈 수 있다는 것을 반드시 먼저 알린다.**

### ★ 재부팅할 때는 **항상 같은 문구를 남긴다**

**사람은 화면이 사라진 것만 본다.** 아무 말 없이 닫으면 「멈췄나」·「죽었나」로 읽힌다.

> ⏳ **유니티를 재부팅 중입니다. 기다려주세요.**

| 언제 | 무엇 |
|---|---|
| **닫기 전** | 위 문구 + **저장 안 된 작업이 사라진다**는 경고 |
| **다시 열 때** | 위 문구 |
| 여러 단계를 이어 돌릴 때 | **마지막 단계에서만 연다** — 중간에 열면 다음 단계가 락에 막힌다 |

러너가 대신 찍는다 — `CLOSE_EDITOR=1` (닫고 시작) · `REOPEN=1` (끝나고 연다).
**말없이 `taskkill` 하지 않는다.**

⚠ **비개발자가 의뢰했다는 사실이 품질을 낮출 이유가 되지 않는다.**
그 사람은 결과물을 검수할 수 없으므로 **기준을 우리가 지킨다.**

## 확정값을 적는 자리

**게임별 확정값(해상도·배율·색공간·합격 기준)은 그 게임의 원장에만 적는다.**
`HtmlToUnity/skills/htmltounity/references/*.md` 는 **게임을 넘어 재사용되는 문서**라
특정 게임의 고유명사·실측치를 적으면 다음 게임에서 오염이 된다 (세트의 금칙).

담당자는 착수 전 3단계에서 원장을 읽으므로 확정표를 자동으로 본다.
